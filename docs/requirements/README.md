# Anforderungs-Register & Rückverfolgbarkeit (Traceability)

> Dieses Verzeichnis ist die Wurzel der **Rückverfolgbarkeit**. Es ist der Kern
> der Zertifizierungs-Fähigkeit (siehe [ADR 0004](../decisions/0004-assurance-und-zertifizierungsfaehigkeit.md)).

## Warum das wichtig ist (fachlich)

Ein Audit nach ED-109A/DO-278A fragt im Kern immer dasselbe: *„Zeig mir für jede
Anforderung, wo sie umgesetzt ist — und wo bewiesen wird, dass sie erfüllt
ist."* Diese durchgehende Kette nennt man **Rückverfolgbarkeit**:

```
Anforderung  →  Design  →  Code  →  Test (Nachweis)
        und zurück (welcher Test deckt welche Anforderung?)
```

Wer das von Anfang an pflegt, hat später kein Drama. Wer es nachrüstet, baut die
Software faktisch neu. Deshalb fangen wir jetzt damit an — schlank, aber echt.

## Wie wir es konkret machen (technisch)

1. **Jede Anforderung bekommt eine stabile ID** in einer von drei Klassen:
   - `FR-…` **Functional Requirement** (was das System *tut*).
   - `NFR-…` **Non-Functional Requirement** (Eigenschaften: Cloud, Performance,
     Security, …).
   - `CON-…` **Constraint** (gesetzte Randbedingung, z. B. Normrahmen).
   IDs werden **nie wiederverwendet**. Wird etwas ungültig, Status → `verworfen`.

2. **Code und Tests verweisen auf die ID** per Tag-Kommentar, z. B.
   `// REQ: FR-GEO-001` an der umsetzenden Stelle bzw. im Test. So findet man per
   Volltextsuche jederzeit alle Berührungspunkte einer Anforderung.

3. **Die Tabelle unten ist die einzige Wahrheit** („single source of truth").
   Spalte *Nachweis* nennt den/die Test(s), die die Anforderung absichern.

Diese Konvention gilt **ab jetzt** für neuen Code. Bestehender M1-Code wird beim
nächsten Anfassen nachgezogen; seine Nachweise stehen bereits in der Tabelle.

## Anforderungs-Register

### Funktional (FR)

| ID | Anforderung | Status | Nachweis (Test) |
|----|-------------|--------|-----------------|
| FR-GEO-001 | Geodätische Umrechnung WGS84↔ECEF↔ENU↔Polar ist verlustfrei (Roundtrip-konsistent). | umgesetzt | `firefly-geo`: `wgs84_ecef_roundtrip`, `enu_geodetic_roundtrip`, `enu_polar_roundtrip` |
| FR-GEO-002 | Azimut wird von Nord im Uhrzeigersinn in [0,2π) geführt. | umgesetzt | `firefly-geo`: `azimuth_conventions` |
| FR-GEO-003 | Eine horizontale Messung (Position + 2×2-Kovarianz) lässt sich von einem `LocalFrame` in einen anderen transformieren: `horizontal_from(source, z, height, r)` nimmt die boden-projizierte Position `z` **plus die Zielhöhe** über der Quell-Tangentialebene, rekonstruiert daraus den **vollständigen 3D-Punkt** `[z.x, z.y, height]`, verkettet ihn exakt geodätisch (Quell-ENU → WGS84 → Ziel-ENU, Ursprungsversatz berücksichtigt) und projiziert **erst im Zielframe** auf den Boden. So ist das horizontale Ergebnis **sensor-unabhängig** — zwei überlappende Radare bilden dasselbe hoch fliegende Ziel auf **denselben** Horizontalpunkt ab (Behebung des **Höhen-Projektionsfehlers**, M6-Befund; eine Boden-Projektion im Quellframe mit `up=0` würde entlang der je Sensor verschiedenen lokalen Vertikalen projizieren und ein 10-km-Ziel um zig Meter zwischen den Radaren versetzen → Geister-Track). Für `height=0` bzw. `source==self` reduziert es sich auf die frühere Bodenebenen-Transformation bzw. die Identität. Kovarianz gedreht um die Frame-Rotation (`R' = T·R·Tᵀ`, T = horizontaler Block von `R_ziel·R_quelleᵀ`). Grundbaustein der zentralen Mess-Fusion (ADR 0010). | verifiziert | `firefly-geo`: `frame_transform::transform_into_same_frame_is_identity`, `frame_transform::position_matches_direct_measurement_in_target_frame` (jetzt mit hoch fliegendem Ziel), `frame_transform::airborne_target_maps_to_one_point_from_two_sensors` (Regressionstest Höhen-Projektionsfehler), `frame_transform::transform_round_trips_between_frames`, `frame_transform::covariance_transform_preserves_ellipse_shape`, `frame_transform::converging_frames_rotate_the_covariance` |
| FR-GEO-004 | Eine WGS84-Position lässt sich konform (winkeltreu) in eine System-Stereografische Ebene projizieren und zurück (Referenzpunkt = Tangentenpunkt der Projektion, X/Y in Metern). Konstruktion: konforme Breite (Ellipsoid → Gaußsche Kugel) gefolgt von sphärischer schiefachsiger stereografischer Projektion (Snyder). Grundlage für CAT062 I062/100 (ADR 0006-Nachtrag). | verifiziert | `firefly-geo`: `stereographic::reference_point_projects_to_origin`, `stereographic::projection_roundtrips`, `stereographic::east_and_north_directions_match_enu_near_reference`, `stereographic::distance_from_reference_matches_great_circle_for_short_ranges` |
| FR-GEO-005 | Es gibt **einen** System-Referenzpunkt je Instanz (Single Source of Truth, ADR 0021), der zugleich Tracking-Frame-Ursprung und I062/100-Projektionsreferenz ist. Default = Mitte der Union-Bounding-Box der konfigurierten Quellen, überschreibbar via `FIREFLY_SYSTEM_REF_LAT/_LON` (seit ADR 0030 der einzige Betrieb; der frühere Replay-Szenen-Ursprung entfiel). Unset/unparsbar fällt je Achse auf den Default zurück. Damit ist I062/100 stets kohärent mit der Track-Berechnung; das frühere unabhängige `FIREFLY_CAT062_REF_*` entfällt. | verifiziert | `firefly-server`: `live::tests::system_reference_point_resolves_override_then_bbox_midpoint`; `firefly-server` (Kohärenz Replay/Encoder): `replay_plots::replayed_snapshot_encodes_to_valid_cat062` |
| FR-SIM-001 | Ziele folgen kinematischen Legs (Cruise/Turn/Climb/Accelerate) korrekt. | umgesetzt | `firefly-sim`: `cruise_*`, `quarter_turn_*`, `climb_*`, `acceleration_*` |
| FR-SIM-002 | Radar erzeugt Plots nur innerhalb von Reichweite und über dem tiefsten Strahl. | umgesetzt | `firefly-sim`: `target_disappears_after_script_ends` (Coverage), Demo |
| FR-SIM-003 | Erfassungswahrscheinlichkeit (Pd) wird statistisch korrekt angewandt. | umgesetzt | `firefly-sim`: `detection_probability_is_respected` |
| FR-SIM-004 | Messrauschen wird im polaren Sensorframe (Range/Azimut/Elevation) aufgeschlagen. | umgesetzt | Code-Review + `radar.rs`-Doku; quantitativer Test offen |
| FR-SIM-005 | SSR-fähige Ziele liefern kombinierte Plots mit Mode-3/A, Flugfläche, ICAO-Adresse. | umgesetzt | `firefly-sim`: `equipped_target_yields_combined_plots_with_ssr` |
| FR-SIM-006 | Der Plot-Strom ist nach Zeit geordnet. | umgesetzt | `firefly-sim`: `plots_are_time_ordered` |
| FR-TRK-001 | Der Tracker bildet aus dem Plot-Strom bestätigte Tracks (Gating, Assoziation, Filter, Lebenszyklus zusammengeführt). | verifiziert | `firefly-track`: `tracker::*`, `lifecycle::single_target_yields_one_confirmed_track`, `lifecycle::two_crossing_targets_keep_their_identities` |
| FR-TRK-002 | Polarer Plot wird in eine kartesische Messung mit Kovarianz umgerechnet (Converted Measurement via Jacobi-Matrix). Die radiale (Bodenentfernungs-)Unsicherheit berücksichtigt **beide** Quellen — Entfernungs- *und* Höhenwinkel-Rauschen, beide auf die Bodenebene projiziert (`σ_ρ² = (cos φ·σ_r)² + (r·sin φ·σ_φ)²`); andernfalls ist das Gate für hoch fliegende Ziele viel zu eng (M3-Befund). | verifiziert | `firefly-track`: `position_matches_geo_conventions`, `elevation_projects_to_ground_range`, `covariance_is_cigar_shaped`, `elevation_noise_inflates_radial_variance`, `cross_range_variance_grows_with_range`, `covariance_is_symmetric_and_positive_definite` |
| FR-TRK-003 | Kalman-Filter (Constant-Velocity) schätzt Position + Geschwindigkeit; Prädiktion vergrößert, Update verkleinert die Unsicherheit; glättet besser als die Rohmessung. | verifiziert | `firefly-track`: `predict_moves_position_and_grows_uncertainty`, `update_reduces_uncertainty`, `gain_respects_measurement_precision`, `covariance_stays_valid`, `tracking::filter_smooths_and_recovers_velocity` |
| FR-TRK-004 | Gating: ein Plot ist für einen Track plausibel, wenn seine quadrierte Mahalanobis-Distanz `d²=yᵀS⁻¹y` die χ²-Schwelle (2 DOF, `γ=−2·ln(1−P_G)`) nicht überschreitet; das Tor ist anisotrop (richtungsabhängig). | verifiziert | `firefly-track`: `threshold_matches_chi_squared_2dof`, `plot_on_prediction_is_accepted`, `gate_is_anisotropic`, `mahalanobis_scales_in_sigma` |
| FR-TRK-005 | Datenassoziation: global kostenminimale 1:1-Zuordnung Tracks↔Plots (Ungarische Methode) auf den gegateten Mahalanobis-Kosten; Reste werden als unzugeordnete Tracks/Plots zurückgegeben. | verifiziert | `firefly-track`: `hungarian_beats_greedy`, `hungarian_3x3`, `associate_matches_gated_plots`, `associate_leaves_ungated_plot_unassigned`, `associate_leaves_starved_track_unassigned`, `associate_handles_empty_inputs` |
| FR-TRK-006 | Track-Lebenszyklus: Geburt (tentativ), Bestätigung per M-aus-N, Coasting bei Fehldetektion, Löschung nach zu vielen Fehltreffern (getrennte Schwellen tentativ/bestätigt). **Identitäts-Stabilität:** unter realistischem Höhenwinkel-Rauschen *und* einem sanften Manöver behält jedes Ziel genau eine Track-ID (kein Zerbrechen, keine Dubletten). | verifiziert | `firefly-track`: `track_is_born_tentative_then_confirmed`, `confirmed_track_coasts_then_dies`, `tentative_track_dies_quickly`, `separated_plots_make_two_tracks`, `lifecycle::*`, `identity::two_aircraft_keep_one_identity_each_under_elevation_noise_and_a_turn`; `firefly-player`: `frankfurt_regression::frankfurt_scene_keeps_one_identity_per_aircraft` (Fixture, ADR 0030) |
| FR-TRK-007 | Güte-Metriken gegen Ground Truth: Positions-RMSE und Track-Kontinuität (Coverage + ID-Wechsel) als reine, wiederverwendbare Bausteine; gegen ein bekanntes Szenario nachgewiesen. | verifiziert | `firefly-track`: `metrics::rmse_is_root_mean_square`, `metrics::rmse_punishes_outliers`, `metrics::rmse_from_points_uses_euclidean_distance`, `metrics::continuity_counts_coverage_not_gaps_as_switches`, `metrics::continuity_counts_id_change_as_switch`, `metrics::continuity_first_assignment_is_not_a_switch`, `metrics::single_target_quality_meets_thresholds` |
| FR-TRK-008 | Der Tracker liefert den safety-relevanten Track-Status explizit aus (ADR 0008): Coasting-Indikator, Update-Alter (Datenzeit seit letztem Treffer) und Positions-Unsicherheit (1σ-Halbachse der Fehlerellipse aus `P`). Die Entscheidung fällt im Tracker; Adapter/ASD stellen nur dar. | verifiziert | `firefly-track`: `kalman::position_uncertainty_is_semi_major_one_sigma`, `tracker::system_tracks_report_coasting_age_and_uncertainty` |
| FR-TRK-009 | Der Tracker übernimmt die SSR-Identität (Mode-3/A-Code, Mode-S-ICAO-Adresse) aus zugeordneten Plots in den Track und gibt sie auf dem `SystemTrack` aus. Die Übernahme ist „sticky": ein Plot ohne SSR-Antwort (z. B. reiner Primär-Treffer) löscht eine bekannte Identität nicht; eine neue SSR-Antwort überschreibt sie. Grundlage für CAT062-Identitätsfelder und die Korrelations-Schlüssel der Multi-Radar-Fusion (M4). | verifiziert | `firefly-track`: `track::fresh_track_has_no_identity`, `track::identity_is_absorbed_from_ssr_reply`, `track::missing_ssr_reply_does_not_clear_known_identity`, `track::new_ssr_reply_overwrites_known_identity`, `tracker::primary_only_track_has_no_identity`, `tracker::ssr_identity_reaches_system_track_and_stays_sticky` |
| FR-TRK-010 | Zentrale Mess-Fusion (ADR 0010): Der Tracker nimmt Plots **mehrerer Sensoren** entgegen. Jeder Plot wird mit dem Rauschmodell **seines** Sensors umgerechnet und (Position + Kovarianz) in einen **gemeinsamen Tracking-Frame** gehoben (FR-GEO-003), bevor gegated/assoziiert wird. Die Sensoren werden je Scan **sequenziell** verarbeitet (ein zweiter Sensor ordnet seinen Plot dem bereits aktualisierten Track zu statt einen Geist zu gebären); Treffer/Fehltreffer wird **einmal pro Scan** gebucht, sodass der M-aus-N-Lebenszyklus für einen Einzelsensor unverändert bleibt. Plots unbekannter Sensoren werden verworfen. Der `SystemTrack` weist zusätzlich aus, **welche Sensoren im letzten Scan beigetragen haben** (`contributing_sensors`, sortiert; leer beim Coasten) — löst die Single-Sensor-Vereinfachung von `update_age`/I062/290 für Multi-Radar auf. | verifiziert | `firefly-track`: `tracker::two_sensors_seeing_one_aircraft_make_one_track`, `tracker::plot_from_unregistered_sensor_is_dropped`, `tracker::system_track_reports_contributing_sensors_per_scan`; `firefly-player`: `multi_radar::two_radars_one_aircraft_yield_one_stable_track` (E2E: Sim → Multi-Sensor-Tracker → Frame-Strom; je 51 Plots beider Radare → **ein** Track); (+ unveränderte `lifecycle::*`, `snapshot::*`, `identity::*` für die Einzelsensor-Äquivalenz) |
| FR-TRK-011 | Der Tracker kennt **mehrere Bewegungsmodelle** (Grundlage für IMM, M5): neben *Constant-Velocity* (geradlinig, gleichförmig) das *Coordinated-Turn*-Modell — ein gleichmäßiger Kurvenflug mit konstanter Drehrate `ω` (rad/s). Beide teilen denselben 4-D-Zustand `[E, N, vE, vN]`, sodass Filter, Gate und Assoziation unverändert mit beiden arbeiten; die CT-Übergangsmatrix dreht den Geschwindigkeitsvektor um `ω·dt` und integriert den Kreisbogen in die Position, geht für `ω → 0` exakt in CV über (Drehzahl-Null-Spezialfall, numerisch abgesichert). `LinearKalman::predict_with(&MotionModel, …)` prädiziert unter einem expliziten Modell; `predict` bleibt der CV-Standard. | verifiziert | `firefly-track`: `motion::zero_turn_rate_is_constant_velocity`, `motion::small_turn_rate_is_close_to_constant_velocity`, `motion::quarter_turn_rotates_velocity_by_ninety_degrees`, `motion::coordinated_turn_conserves_speed` |
| FR-TRK-012 | IMM-Grundgerüst (Häppchen M5.2): Der Tracker hält eine **Bank** von Kalman-Filtern unter verschiedenen Bewegungsmodellen mit je einer **Modellwahrscheinlichkeit** `μ` und einer **Markov-Übergangsmatrix** `π_ij` (zeilenstochastisch). Der **Mischungs-/Interaktions-Schritt** berechnet die prädizierten Modellwahrscheinlichkeiten `c_j = Σ_i π_ij·μ_i`, daraus die Mischungsgewichte `μ_{i|j} = π_ij·μ_i/c_j` und für jedes Zielmodell `j` die gemischte Anfangsbedingung `x_0j = Σ_i μ_{i|j}·x_i`, `P_0j = Σ_i μ_{i|j}·[P_i + (x_i−x_0j)(x_i−x_0j)ᵀ]` (Kovarianz mit „Spread-of-the-Means"-Term). Reine, deterministische Funktion (`Imm` in `firefly-track`). | verifiziert | `firefly-track`: `imm::predicted_model_probabilities_follow_the_markov_chain`, `imm::mixing_weights_are_proper_distributions`, `imm::identity_transition_leaves_states_unmixed`, `imm::mixing_blends_means_and_inflates_covariance`, `imm::mixing_identical_states_is_a_noop` |
| FR-TRK-013 | Vollständiger IMM-Zyklus (Häppchen M5.3): Jedes Modell filtert aus seiner gemischten Anfangsbedingung (`predict_with` + ggf. `update`) und liefert bei einem Treffer eine Gauß-**Likelihood** `Λ_j = N(y_j; 0, S_j)` (`LinearKalman::measurement_likelihood`, Normierer `2π·√|S|` für 2-D-Messungen). Das **Modellwahrscheinlichkeits-Update** gewichtet `μ_j ∝ c_j·Λ_j` (das Modell, das den Plot am besten vorhersagt, gewinnt); beim Coasten relaxieren die Wahrscheinlichkeiten zur Markov-Prädiktion `c_j`. Die **kombinierte Schätzung** ist die mit `μ` gewichtete Mischung (`combined_estimate`, Kovarianz mit Spread-of-the-Means-Term). `Imm::step(dt, Q, Option<&measurement>)` führt einen ganzen Zyklus aus. | verifiziert | `firefly-track`: `imm::combined_estimate_is_the_weighted_blend`, `imm::step_keeps_probabilities_normalised`, `imm::constant_velocity_model_wins_on_a_straight_track`, `imm::coordinated_turn_model_wins_on_a_turning_track`, `imm::coasting_relaxes_toward_markov_prediction` |
| FR-TRK-014 | IMM im Tracker (Häppchen M5.4): Jeder Track trägt statt eines einzelnen Kalman-Filters eine **IMM-Bank** (`Imm`, konfiguriert über `ImmConfig` in `TrackerConfig`; Standard `cv_and_turns`: CV + Coordinated-Turn ±0,052 rad/s ≈ ±3°/s, klebrige Markov-Matrix). Der Pro-Scan-Ablauf bleibt unverändert: **Prädiktion** ruft `Imm::predict` (Mischung + modellweise Prädiktion), **Gating/Assoziation** laufen über die kombinierte Schätzung (`Track::estimate`), **Update** ruft `Imm::update` (Likelihood-Gewichtung der Modelle). Ein neuer Track wird mit `ImmConfig::seed` aus der ersten Messung geboren. Der `SystemTrack`-Output (Position/Geschwindigkeit/Unsicherheit) kommt aus der kombinierten Schätzung; das Single-Sensor- und Multi-Radar-Verhalten bleibt für Geradeausflug erhalten (RMSE-/Identitäts-Tests grün). | verifiziert | `firefly-track`: `tracker::imm_favours_the_turn_model_on_a_turning_target` (E2E: kurvendes Ziel → CT-Modell dominiert) + alle unveränderten `tracker::*`, `metrics::single_target_quality_meets_thresholds`, `identity::*`; `firefly-player`: `multi_radar::*` |
| FR-TRK-015 | Probabilistische Datenassoziation (PDA, Häppchen M5.5): Für einen Track und die in seinem Tor liegenden Plots werden **Assoziationswahrscheinlichkeiten** `β` berechnet — `β_0` für „kein Treffer" (Clutter/Fehldetektion), `β_j` für „Plot `j` ist die wahre Rückmeldung". Formel `β_j = Λ_j/(b+ΣΛ_i)`, `β_0 = b/(b+ΣΛ_i)` mit Clutter-Term `b = λ·(1−P_D·P_G)/P_D` (`ClutterModel`: Clutter-Dichte `λ`, Detektionswahrscheinlichkeit `P_D`; `P_G` aus der Gate-Schwelle, `P_G = 1−e^{−γ/2}`). Die `β` summieren sich stets zu 1. | verifiziert | `firefly-track`: `pda::empty_gate_means_no_detection`, `pda::betas_sum_to_one`, `pda::perfect_plot_with_little_clutter_dominates`, `pda::more_clutter_increases_no_detection_probability`, `pda::closer_plot_gets_more_weight` |
| FR-TRK-016 | PDA-gewichtetes Kalman-Update (Häppchen M5.6): `LinearKalman::update_pda` faltet **alle** gegateten Plots auf einmal in den Filter, statt sich auf einen festzulegen. Jede Hypothese — „kein Treffer" (Zustand bleibt die Prädiktion) oder „Plot `j` gehört dazu" (gewöhnliches `update(j)`) — liefert einen Kandidatenzustand; das Ergebnis ist die `β`-gewichtete Mischung (`x = Σβ_k·x_k`, `P = Σβ_k·(P_k+d_k·d_kᵀ)`, „Spread-of-the-Means", analog zur IMM-Mischung aus FR-TRK-012). Mit `betas=[1.0]` (keine Messung) ein Noop; mit `betas=[0,1]` (sicherer Treffer) identisch zu `update`. | verifiziert | `firefly-track`: `kalman::pda_update_with_no_measurements_is_a_noop`, `kalman::pda_update_with_certain_single_measurement_matches_plain_update`, `kalman::pda_update_blends_disagreeing_measurements_and_inflates_uncertainty`, `kalman::pda_update_keeps_covariance_valid` |
| FR-TRK-017 | PDA-gewichtetes IMM-Update (Häppchen M5.7): `Imm::update_pda` überträgt FR-TRK-016 auf die ganze Modell-Bank. Zweig 0 = die Bank unverändert wie nach `predict` (kein Treffer); Zweig `1+j` = ein vollständiges `Imm::update(measurements[j])` auf einer Kopie der Bank (mit eigener Modellwahrscheinlichkeits-Neubewertung). Die neue Bank mischt je Modell die Zustände (Spread-of-the-Means) und mischt+normalisiert die Modellwahrscheinlichkeiten `β`-gewichtet über alle Zweige. Randfälle wie bei FR-TRK-016 (Noop bzw. Äquivalenz zu `update`) gelten analog. | verifiziert | `firefly-track`: `imm::pda_update_with_no_measurements_is_a_noop`, `imm::pda_update_with_certain_single_measurement_matches_plain_update`, `imm::pda_update_keeps_probabilities_normalised` |
| FR-TRK-018 | Joint Probabilistic Data Association (JPDA, Häppchen M5.8): `joint_association_probabilities` erweitert die Pro-Track-PDA (FR-TRK-015) auf **mehrere Tracks mit überlappenden Toren** durch eine **Exklusivitäts-Bedingung** — ein Plot kann pro „gemeinsamem Ereignis" höchstens einem Track zugeordnet sein. Tracks/Plots, die über gemeinsame gegatete Plots verbunden sind, bilden über Union-Find ein **Cluster**; pro Cluster werden alle zulässigen gemeinsamen Zuordnungen erschöpfend aufgezählt (`ClusterEnumerator`, Gewicht je Ereignis `∏(Λ_ij wenn zugeordnet, sonst b)`) und zu `β_ij` marginalisiert (Summe der Ereignisgewichte mit `i↔j`, normiert). Für `t=1` fällt die Formel exakt auf FR-TRK-015 zurück. | verifiziert | `firefly-track`: `jpda::rows_sum_to_one`, `jpda::single_track_matches_per_track_pda`, `jpda::track_with_no_gated_plot_is_independent`, `jpda::overlapping_tracks_split_a_shared_plot`, `jpda::no_measurements_means_certain_no_detection` |
| FR-TRK-019 | JPDA im Tracker (Häppchen M5.9): Die harte 1:1-GNN-Zuordnung (FR-TRK-005) ist je Sensor durch JPDA (FR-TRK-018) ersetzt — jeder Track faltet **alle** seine gegateten Plots auf einmal über `Imm::update_pda` (FR-TRK-017) ein, gewichtet mit den gemeinsamen `β`. Ein Track gilt als getroffen (`last_hit_time`, `record_hit_from`), wenn `β_0 < 1−ε`; der Plot mit dem größten `β` dient der Identitäts-Übernahme (FR-TRK-009), ohne Teil der kinematischen Mischung zu sein. Neue Tracks entstehen aus Plots, die in **keinem** Tor landen. `TrackerConfig` trägt dafür ein `ClutterModel` (Default: ~1 Falschplot/10 km², `P_D=0,95`). Zwei eng benachbarte, parallele Ziele mit überlappenden Toren bleiben über alle Scans als **zwei** unterscheidbare, bestätigte Tracks erhalten — eine gewisse Annäherung („Track-Koaleszenz") ist dabei ein bekanntes, dokumentiertes JPDA-Merkmal. | verifiziert | `firefly-track`: `tracker::jpda_keeps_two_close_parallel_tracks_distinct` sowie alle unveränderten `tracker::*`, `metrics::single_target_quality_meets_thresholds`, `identity::*` |
| FR-TRK-020 | Robuste Multi-Radar-Scan-Verarbeitung gegen Geister-Tracks (ADR 0011): (a) **Gemeinsame, zu Scan-Beginn eingefrorene Fusions-Referenz** — alle Sensoren eines Scans gaten/assoziieren gegen die Track-Schätzungen zu Scan-Beginn, nie gegen den schon aktualisierten Live-Track, sodass die sequenzielle Verarbeitung das Tor des nächsten Sensors nicht verengt; ein **während** des Scans neu geborener Track wird erst *nach* dem Sensor-Block an die Referenz angehängt (vetoiert den nächsten Sensor → ein Flugzeug/zwei Radare = ein Track, nicht die Geschwister-Plots desselben Sensors). Die Zustands-Fusion bleibt sequenziell (für unabhängige Messungen = gemeinsames Update). (b) **Getrenntes Initiierungs-Sperr-Tor** `init_gate` (Default `P_G=0,9999`), weiter als das Assoziations-Tor `gate` (`0,99`): ein Plot im weiteren Tor eines bestehenden Tracks startet **keinen** neuen Track (er ist ein Ausreißer eines bekannten Ziels), während die Assoziation das enge Tor behält. Behebt beide in M6.1 gefundenen Geister-Ursachen ohne das Assoziations-Tor zu weiten; die Showcase-Szene läuft wieder mit Standard `0,99`. | verifiziert | `firefly-track`: `tracker::outlier_plot_does_not_spawn_a_ghost`, sowie unverändert grün `tracker::two_sensors_seeing_one_aircraft_make_one_track`, `tracker::jpda_keeps_two_close_parallel_tracks_distinct`; `firefly-player`: `frankfurt_regression::frankfurt_scene_keeps_one_identity_per_aircraft` (Fixture-Umzug, ADR 0030) (8 Tracks bei Gate `0,99`) |
| FR-TRK-021 | Adaptiver Track-Lebenszyklus für asynchrone Multi-Radar-Feeds (ADR 0012): Bestätigungs- und Löschfenster (`confirm_n`, `delete_misses_*`) werden nicht mehr in festen Scan-Aufrufen, sondern in `coast_reference = max(revisit_interval, cadence)` Sekunden gezählt — `revisit_interval` ist ein EWMA der Treffer-Zeitlücken je Track (`mark_hit`), `cadence` die vom Tracker geschätzte Feed-Kadenz (max. aus Inter-Scan-Lücke und größter bekannter Sensor-Scan-Periode). Bestätigung: `hits_within(confirm_n · coast_reference, t) ≥ confirm_m`. Löschung: `update_age ≥ delete_misses_* · coast_reference`. Bootstrap-Sonderfall: solange noch keine Sensor-Periode bekannt ist, gilt `cadence = ∞` (keine Löschung), damit ein im ersten Augenblick geborener Track nicht gelöscht wird, bevor sein eigener Sensor überhaupt erneut scannt. Behebt Track-Churn bei versetzten Radar-Scanzeiten (28 statt 8 Track-IDs in der Frankfurt-Szene) ohne NFR-CLOUD-004 zu verletzen. | verifiziert | `firefly-track`: `track::*` (neu: `mark_hit`, `coast_reference`, `hits_within`), alle `tracker::*` inkl. `tentative_track_dies_quickly`, `confirmed_track_coasts_then_dies`; `timing::long_gap_with_data_keeps_track_identity`, `timing::deletion_is_governed_by_miss_count_not_elapsed_time`; `firefly-player`: `frankfurt_regression::frankfurt_scene_keeps_one_identity_per_aircraft` (Fixture-Umzug, ADR 0030) (8 Track-IDs bei versetzten Radar-Scanzeiten `scan_offset = 0/1.3/2.6`) |
| FR-TRK-022 | Asynchrone Pro-Plot-Verarbeitung (ADR 0013, Häppchen 13.1, **additiv**): `Tracker::process_plot(plot)` verarbeitet **einen einzelnen Plot zu seiner eigenen Datenzeit** als Gegenstück zum Batch-`process_scan`. Ablauf: (1) **alle** Tracks auf die Plot-Zeit prädizieren (nicht auf eine gemeinsame Scan-Zeit); (2) den Plot gegen die **Live-Schätzungen** gaten/assoziieren — **ohne** eingefrorene Scan-Start-Referenz, da zeitlich getrennte (asynchrone) Plots durch die Prädiktions-Kovarianz natürlich wieder ins Tor wachsen, sodass die von ADR 0011 (FR-TRK-020) bekämpfte sequenzielle Tor-Verengung hier nicht entsteht; (3) jeden Track, in dessen Tor der Plot fällt, per PDA über die *eine* Messung (mit JPDA-Exklusivität über Tracks) **updaten** — oder einen neuen tentativen Track **initiieren**, wenn der Plot in **keinem** (weiteren) `init_gate` liegt; (4) zeit-skalierte Bestätigung/Löschung wie FR-TRK-021. Reine Funktion von Zustand + Plot-Datenzeit (Determinismus, NFR-CLOUD-001). Koexistiert additiv mit `process_scan` (Batch-Pfad unverändert; die asynchrone Ausgabe-Pipeline schaltet in 13.4/13.5 um). Feed-Kadenz hier = größte Plot-zu-Plot-Lücke je Sensor, `∞` bis bekannt (Bootstrap wie FR-TRK-021). | verifiziert | `firefly-track`: `tracker::process_plot_births_then_confirms`, `tracker::process_plot_separated_plots_make_two_tracks`, `tracker::process_plot_honours_each_plots_own_time`, `tracker::process_plot_track_coasts_when_unseen`, `tracker::process_plot_absorbs_and_keeps_identity`, `tracker::process_plot_deletes_unseen_tentative`, `tracker::process_plot_from_unregistered_sensor_is_dropped`; sämtliche bestehenden `tracker::*` (Batch-Pfad) unverändert grün |
| FR-TRK-023 | Zeit-kontinuierlicher Track-Lebenszyklus im asynchronen Pfad (ADR 0013, Häppchen 13.2): Im `process_plot`-Pfad werden Bestätigung und Löschung allein durch die **eigene** Revisit-Kadenz jedes Tracks skaliert — `Track::expected_revisit(nominal)` liefert das gemessene `revisit_interval`, solange bekannt, sonst `NOMINAL_REVISIT_INTERVAL` (5 s) als Bootstrap für einen noch nicht erneut gesehenen Track. **Keine** global geschätzte Feed-Kadenz mehr auf diesem Pfad: die `sensor_period`/`sensor_last_scan`-Buchhaltung wird in `process_plot` nicht mehr berührt (sie bleibt ausschließlich für den Batch-`process_scan`/FR-TRK-021). Bestätigung: `hits_within(confirm_n · expected_revisit, t) ≥ confirm_m`. Löschung (`should_delete_continuous`): `update_age ≥ delete_misses_* · expected_revisit`. Vereinfacht den ADR-0012-Workaround für den async-Pfad — ein Track lebt/stirbt nach echten Update-Lücken statt nach Scan-Aufrufen oder Feed-Takt. `process_scan` (Batch) und die Frankfurt-Szene bleiben unverändert (Ansatz B); Zusammenführung beider Pfade in 13.4/13.5. | verifiziert | `firefly-track`: `track::expected_revisit_falls_back_to_nominal_until_established`, `tracker::process_plot_deletes_unseen_tentative` (nominal-basiert), `tracker::process_plot_two_async_sensors_make_one_track`, sowie alle übrigen `tracker::process_plot_*`; sämtliche Batch-`tracker::*` und `firefly-player::frankfurt_regression::frankfurt_scene_keeps_one_identity_per_aircraft` (Fixture-Umzug, ADR 0030) unverändert grün |
| FR-TRK-024 | Read-only Zeit-Snapshot (`Tracker::snapshot_at(t)`, ADR 0013, Häppchen 13.3): projiziert **alle** Tracks auf eine gemeinsame Ausgabezeit `t`, indem je Track die IMM-Bank **auf einer Kopie** auf `t` prädiziert wird (IMM-Mischung + Dead-Reckoning, nur für `dt = t − last_time > 0`, sonst aktuelle Schätzung), und liefert neutrale `SystemTrack`s mit konsistentem Zeitstempel `t`, `update_age = t − last_hit_time` und `coasting = update_age ≥ expected_revisit` (der Track ist überfällig um mindestens ein volles erwartetes Revisit-Intervall — `revisit_interval`, sonst `NOMINAL_REVISIT_INTERVAL`). **Korrektur:** zuvor `coasting = t > last_hit_time`, was im entkoppelten Ausgabetakt (13.4) faktisch *jeden* Track als coastend meldete, weil der feste Herzschlag praktisch immer eine Spur nach dem letzten Treffer eines auch frisch aktualisierten Tracks liegt; das Lagebild coastete dauerhaft. **Verändert den Tracker-Zustand nicht** — kein `predict` am echten Track, kein Treffer, keine Löschung; ein Snapshot weit in der Zukunft löscht nichts und ein wiederholter Snapshot ist identisch. Grundlage für den entkoppelten periodischen Ausgabetakt mit festem Herzschlag (13.4). Die WGS84-Projektion teilt sich `snapshot_at` mit `system_tracks` über die freie Funktion `system_track_from`, sodass beide Ausgabe-Ports nicht divergieren. | verifiziert | `firefly-track`: `tracker::snapshot_at_predicts_forward_and_is_read_only`, `tracker::snapshot_at_reports_age_and_coasting_at_output_time`, `tracker::snapshot_at_does_not_coast_a_freshly_updated_track`, `tracker::snapshot_at_on_empty_tracker_is_empty`; die `tracker::system_track*`-Tests bestätigen die verhaltensgleiche Extraktion von `system_track_from` |
| FR-TRK-025 | Gemeinsame Assoziation über *nahezu gleichzeitige* Plots im asynchronen Pfad (ADR 0013, Häppchen 13.5): `Tracker::process_plots(plots)` verarbeitet eine Folge asynchroner Plots mit **je eigener Datenzeit** und verfeinert die Pro-Plot-Assoziation aus FR-TRK-022. Plots, deren Zeiten innerhalb des **Simultaneitäts-Fensters** (`SIMULTANEITY_WINDOW`, 0,5 s) ab Gruppen-Beginn liegen, bilden **eine Mess-Gelegenheit** und werden **gemeinsam** gegen eine an der Gruppen-Zeit eingefrorene Referenz assoziiert — ADR-0011-Geisterunterdrückung (FR-TRK-020) **plus** JPDA-Exklusivität (FR-TRK-018/019), exakt wie ein Scan. Damit bleiben zwei sich kreuzende Ziele, die ein Radar bei nahezu gleichem Azimut meldet, ebenso wie zwei Radare, die ein Ziel im selben Moment malen, identitätstreu bzw. geisterfrei. Zeitlich weiter getrennte Gruppen sind unabhängige Gelegenheiten (Kovarianz wächst über die Prädiktions-Lücke natürlich wieder ins Tor — kein eingefrorener Bezug nötig). Der Assoziations-Kern (`fuse_simultaneous_plots`) ist aus `process_scan` **extrahiert** und wird von Batch- und async-Pfad geteilt (eine einzige sicherheitskritische Fusions-Logik); `process_scan` bleibt verhaltensgleich, `process_plot` ist die Ein-Plot-Bequemlichkeit über `process_plots`. Verarbeitung in totaler Ordnung (Datenzeit, dann Sensor-ID) → eingabe-reihenfolge-unabhängig (Determinismus, NFR-CLOUD-001). Generalisiert den Exakt-gleiche-Zeit-Scan auf azimut-verteilte Plot-Zeiten (Häppchen 13.6). | verifiziert | `firefly-track`: `tracker::process_plots_fuse_simultaneous_two_radars_into_one_track`, `tracker::process_plots_keep_crossing_targets_distinct`, `tracker::process_plots_equals_repeated_process_plot_when_well_separated`; alle bestehenden `tracker::*` (Batch-Pfad verhaltensgleich nach Extraktion) und `tracker::process_plot_*` unverändert grün |
| FR-TRK-026 | Kadenz-Boden im async-Lösch-Lebenszyklus (ADR 0013, Häppchen 13.5c, neu kalibriert in 13.5d): `should_delete_continuous` löscht einen Track erst, wenn er um `budget · max(eigene Revisit-Schätzung, langsamste konfigurierte Sensorperiode)` überfällig ist. Die **konfigurierte** Antennenumlaufzeit jedes Sensors (`SensorModel::scan_period`, Teil der Sensor-Deklaration in `TrackerConfig`) ersetzt die in 13.5c online aus Inter-Plot-Lücken geschätzte Periode: Häppchen 13.6s azimut-abhängige Plot-Zeiten machten diese Online-Schätzung systematisch zu kurz (sie erfasste Lücken *zwischen* Sensoren statt eines Sensors eigenen Umlauf). `process_plots` bildet daraus einmalig das Maximum über alle konfigurierten Sensoren als **Boden** (`cadence`). Verfeinert FR-TRK-023: ein Track, den ein schneller Sensor kurz sah (kurze Eigen-Revisit-Schätzung) und der danach nur noch von einem langsamen Radar (z. B. 12 s) abgedeckt wird, wird **nicht** in der langen Lücke weggelöscht und neu geboren — der zeit-kontinuierliche Gegenpart zu ADR 0012s `coast_reference`. Behebt den Haupt-Treiber der Frankfurt-ID-Inflation (22→8) im async-Pfad. Batch-`process_scan`/`should_delete`/`sensor_period`-Online-Schätzung dort unverändert. | verifiziert | `firefly-track`: `tracker::process_plots_cadence_floor_survives_a_slow_sensor_gap` (Sensoren mit konfigurierten Perioden 2 s/12 s); `tracker::process_plot_deletes_unseen_tentative` (nominal-Rückfall, Boden ändert nichts) und alle übrigen `tracker::process_plot*`/`process_plots_*` unverändert grün; `firefly-player`: `frankfurt_regression::frankfurt_scene_keeps_one_identity_per_aircraft` (Fixture-Umzug, ADR 0030), `frankfurt_regression::frankfurt_crossing_pair_keeps_identity_through_the_crossing` |
| FR-TRK-027 | Vertikallage als Pass-through (ADR 0015): `SystemTrack` trägt `flight_level_ft: Option<f64>` (zuletzt gemessene Mode-C-Höhe in Fuß). `Track::update_identity` übernimmt die Flugfläche aus jedem SSR-Plot (`ModeAC.flight_level_ft`) „sticky" — ein Primär-only-Treffer löscht den letzten bekannten Wert nicht — und `system_track_from` reicht sie in den neutralen `SystemTrack` durch. **Kein** vertikaler Kalman-Zustand; eine gemessene, keine geschätzte Größe. Grundlage für CAT062 I062/136 (FR-IO-003). | verifiziert | `firefly-track`: `tracker::measured_flight_level_reaches_system_track_and_stays_sticky` |
| FR-TRK-028 | Callsign (Flight ID / Target Identification) als Pass-through (AP7): `SystemTrack` trägt `callsign: Option<Callsign>` (zuletzt empfangene Mode-S-Kennung, 8 IA-5-Zeichen). `Track::update_identity` übernimmt das Callsign aus jedem SSR-Plot (`ModeAC.callsign`) „sticky" — wie `mode_3a` und `flight_level_ft` löscht ein Primär-only-Treffer den letzten bekannten Wert nicht — und `system_track_from` reicht es in den neutralen `SystemTrack` durch. Reiner Durchreich-Wert, von Firefly nicht generiert oder validiert. Grundlage für CAT062 I062/245 (FR-IO-003). | verifiziert | `firefly-track`: `track::identity_is_absorbed_from_ssr_reply`, `track::missing_ssr_reply_does_not_clear_known_identity` |
| FR-TRK-031 | ICAO-Adress-basierte Vorsortierung (AP9.3): Vor dem kinematischen JPDA-Assoziationsschritt prüft der Tracker, ob ein eingehender Plot eine ICAO-24-Bit-Adresse trägt, die mit einem lebenden Track übereinstimmt. Trifft das zu (genau ein Match), wird der Plot **direkt** diesem Track zugeordnet und aus dem JPDA-Pool entfernt — **seit SPEC.1 nur noch innerhalb des kinematischen Gates** (FR-TRK-045): Identität ist ein weicher Schlüssel, ein ICAO-Match weit außerhalb des Gates ist Duplikat-Evidenz statt autoritativer Zuordnung. Der Track wird mit `update_pda(&[cm], &[0.0, 1.0])` (sichere Assoziation, β=1) aktualisiert, als Hit markiert und die Identität übernommen. Stimmt keine Track-ICAO überein (unbekanntes Luftfahrzeug) oder gibt es mehr als einen Match (inkonsistenter Zustand, defensiv), geht der Plot normal in den JPDA-Pool. Die ICAO-Vorsortierung läuft in beiden Pfaden (`process_scan` / `process_plots`) über den gemeinsamen Kern `fuse_simultaneous_plots`. Die gefrorene Referenz (ADR 0011 Geisterunterdrückung) wird VOR der Vorsortierung erstellt, sodass das Gating der verbleibenden JPDA-Plots unberührt bleibt. Sicherheitshinweis (ADR 0019): ICAO-Adressen sind nicht kryptografisch authentifiziert; Netz-Isolation (ADR 0017) ist die primäre Schutzmaßnahme. | verifiziert (AP9.3) | `firefly-track`: `tracker::icao_match_inside_the_gate_still_associates_directly`, `tracker::icao_no_match_falls_through_to_jpda` (Gate-Revision: `tracker::icao_match_is_gated_and_duplicates_stay_separate`, FR-TRK-045) |
| FR-TRK-030 | ADS-B-Plot-Aufnahme (AP9.1/AP9.2): Der Tracker akzeptiert neben Radar-Polar-Plots auch **geodätische Selbstberichte** (ADS-B). `firefly-core::Measurement` ist ein Enum mit `Polar(Polar)` (bisheriger Pfad) und `Geodetic { position: Wgs84, sigma_pos_m: f64 }` (ADS-B). `Plot::adsb` erzeugt einen ADS-B-Plot mit `DetectionKind::Secondary`. In `firefly-track` dispatcht `tracking_measurement` auf die Enum-Variante: der Polar-Pfad läuft weiter wie zuvor durch `convert_plot` + `horizontal_from`; der Geodetic-Pfad wandelt die WGS84-Position direkt per `tracking_frame.geodetic_to_enu` um und setzt eine isotrope Kovarianz `R = σ² · I₂` (kein Radarsensor-Geometrie-Term). Der Sensor-Frame und das Rauschmodell werden für den Geodetic-Pfad nicht benötigt. `tracking_measurement` ersetzt `convert_plot` an allen Aufrufstellen im Tracker (Batch- und Async-Pfad). | verifiziert (AP9.1 + AP9.2) | `firefly-track`: `measurement::geodetic_at_origin_gives_zero_position_and_isotropic_covariance`, `measurement::geodetic_position_maps_north`, `measurement::geodetic_output_independent_of_sensor_frame_and_model` |
| FR-TRK-033 | Robustheit gegen verspätete und umsortierte Plots (Roadmap-Paket 5): Der Tracker lehnt Eingaben ab, die den Datenzeit-Höchstwert nicht überschreiten, statt korrupte Rückwärts-Prädiktionen auszuführen. (a) `process_plots` sortiert die übergebene Plot-Menge vor der Verarbeitung nach Datenzeit aufsteigend — das Ergebnis ist damit reihenfolge-unabhängig von der Eingabe. (b) Jede Gruppe, deren Datenzeit **strikt kleiner** als der `data_time_watermark` ist, wird verworfen (kein Kalman-Update ohne vorherige Vorwärtsprädikation) und per `tracing::warn!` gemeldet, sodass der Betreiber systematische Sensor-Verzögerungen erkennt. Gleichzeitige Gruppen (`t == watermark`, dt = 0) sind erlaubt — Prädikation ist ein Noop, Messung wird noch eingefaltet (simultanes Multi-Sensor-Treffer-Szenario). (c) `process_scan` verwirft backward-time- oder gleich-time-Scans analog. (d) `kalman::predict_with` trägt `debug_assert!(dt >= 0.0)` als Absicherung für verbleibende Aufrufpfade. (e) `track::hits_within` prüft die `recent_hits`-Sortierinvariante per `debug_assert`. | verifiziert | `firefly-track/tests/timing.rs`: `unsorted_plot_batch_gives_same_result_as_sorted_batch`, `late_plot_batch_is_dropped_gracefully`, `backward_scan_is_dropped_gracefully` |
| FR-TRK-032 | ADS-B-Trefferzeit-Durchreichung (AP9.6): `Track.adsb_last_hit_time: Option<f64>` speichert den Datenzeit-Zeitstempel des letzten ADS-B-Treffers (ICAO-Pre-Sort-Pfad in `fuse_simultaneous_plots`). `SystemTrack.adsb_age_s: Option<f64>` gibt das aus Sicht des Berichts-Zeitpunkts berechnete Alter `(time - hit).max(0.0)` an. `None` wenn der Track noch nie per ADS-B aktualisiert wurde; `Some(age)` wenn mindestens ein ADS-B-Treffer vorliegt. Grundlage für das I062/290 ES-Age-Subfeld im CAT062-Encoder (FR-IO-003, ICD 2.4.0, AP9.5) und für das ADS-B-Badge in Wayfinder (AP9.9). | verifiziert (AP9.6+AP9.7) | `firefly-asterix`: `cat062::update_ages_es_round_trip`, `cat062::update_ages_no_es_subfield_when_adsb_age_absent` (indirekt: prüft, dass ohne ADS-B-Treffer kein ES-Age encodiert wird), `cat062::single_track_with_adsb_hit_matches_reference_dump` (byte-genauer Referenz-Dump, AP9.7), `cat062::decode_recovers_adsb_age_when_present` |
| FR-TRK-034 | Per-Technologie-Provenienz (ADR 0027, #30): Der Tracker führt die Track-Herkunft **autoritativ** je Surveillance-Technologie statt sie dem Konsumenten zu überlassen. `firefly-core::SourceKind { Psr, Ssr, ModeS, AdsB, Flarm }` am `Plot.source` benennt die Technologie-Familie explizit (Produzenten setzen sie: Simulator PSR/SSR/ModeS, `firefly-opensky` AdsB, `firefly-flarm` Flarm); `DetectionKind` bleibt die Radar-Dwell-Korrelation. Der `Track` bucht je Technologie eine letzte-Treffer-Datenzeit (`SourceHits`, generalisiert das frühere `adsb_last_hit_time`); ein kombinierter PSR+SSR/Mode-S-Dwell (`kind.has_primary()`) bucht **beide** Technologien. An allen drei Treffer-Stellen (ICAO-Pre-Sort, JPDA-Best, Track-Geburt) ruft der Tracker `record_source_hit`. `SystemTrack.source_ages: SourceAges` (je `psr`/`ssr`/`mode_s`/`adsb`/`flarm` ein `Option<f64>`-Alter zur Berichtszeit) wird in `system_track_from` berechnet; `adsb_age_s` bleibt Alias für `source_ages.adsb`. `SystemTrack::provenance() -> Provenance` leitet die dominante Herkunft ab: ≥ 2 Technologien **frisch** (Alter ≤ `PROVENANCE_FRESH_S` = 30 s) → `Combined`; sonst die einzelne frische; sonst die jüngste; sonst `Unknown`. Grundlage für CAT062 I062/290 SSR/MDS/FLARM-Subfelder (FR-IO-003, ICD 2.6.0) und den JSON-Pfad (FR-IO-001). | verifiziert | `firefly-core`: `system_track::provenance_unknown_without_any_age`, `system_track::provenance_single_fresh_technology`, `system_track::provenance_two_fresh_technologies_combine`, `system_track::provenance_falls_back_to_most_recent_when_all_stale`, `system_track::provenance_boundary_age_counts_as_fresh`; `firefly-track`: `tracker::primary_only_track_records_psr_age_and_provenance`, `tracker::combined_mode_s_dwell_books_psr_and_mode_s_and_combines` |
| FR-TRK-035 | Verwalteter Track-Nummern-Pool für I062/040 (ARTAS-Roadmap QW.1, ICD 3.1.1): Die 16-Bit-**Draht**-Track-Nummer ist von der internen, prozess-eindeutigen `TrackId` (u32) getrennt und wird aus einem verwalteten Pool vergeben statt per stiller `u32→u16`-Trunkierung abgeleitet (die nach 65 536 Track-Geburten auf dem Draht kollidieren konnte: zwei lebende Tracks unter einer Nummer, TSE löscht beim Konsumenten den falschen Track). `firefly-track::track_number::TrackNumberPool`: (a) frische Nummern aufsteigend ab 1, `0` wird nie vergeben (Sentinel); (b) bei Track-Löschung wird die Nummer zum Löschzeitpunkt (Datenzeit) für `TRACK_NUMBER_QUARANTINE_SECS` = 60 s **quarantänisiert** und erst danach FIFO wiederverwendet — ein Konsument kann eine wiederkehrende Nummer nie mit dem beendeten Track verwechseln; (c) bei erschöpftem Nummernraum (> 65 535 gleichzeitig belegte + quarantänisierte Nummern) wird die Track-Initiierung mit `tracing::warn!` abgelehnt statt eine Duplikat-Nummer zu emittieren (ehrliche Grenze). `Track.number`/`SystemTrack.track_number` tragen die Pool-Nummer; der CAT062-Encoder kodiert I062/040 aus `track_number`, nie aus `id`. Der Pool ist Teil des serialisierbaren Tracker-Zustands (ADR 0007). Wire-Format unverändert (u16 BE, dokumentarische ICD 3.1.1). | verifiziert | `firefly-track`: `track_number::fresh_numbers_ascend_from_one`, `track_number::freed_number_waits_out_quarantine`, `track_number::reuse_is_fifo_by_release_time`, `track_number::exhausted_pool_never_duplicates`, `track_number::fresh_space_ends_at_u16_max`, `tracker::track_number_is_quarantined_after_deletion_then_reused`; `firefly-asterix`: `cat062::track_number_is_pool_number_not_truncated_id` |
| FR-TRK-036 | Vertrauens-Flags im Track-Status (ARTAS-Roadmap QW.3, ICD 3.2.0, additiv): (a) **MON (monosensor):** Der `Track` bucht je **distinktem Sensor** die letzte Treffer-Datenzeit (`sensor_hits`, persistent — anders als das pro-Scan geleerte `contributing_sensors`, das auf einem asynchronen Multi-Radar-Feed flattern würde); `fresh_sensor_count(now, window)` zählt die Sensoren im Frische-Fenster (`PROVENANCE_FRESH_S` = 30 s, dieselbe Frische-Semantik wie die Provenienz FR-TRK-034). `SystemTrack.monosensor = fresh_sensor_count ≤ 1` (lange coastende Tracks eingeschlossen) → CAT062 I062/080 Oktett 1 Bit 8 (`0x80`). (b) **SPI (Special Position Identification):** `ModeAC.spi` wird vom CAT048-Decoder aus I048/020 Oktett 1 Bit 3 (`TRD_SPI = 0x04`) gelesen und vom `radar_asterix`-Adapter durchgereicht (ADS-B-/FLARM-Adapter: `false`); am `Track` bewusst **transient** (jede assoziierte Meldung überschreibt, `update_identity`) → `SystemTrack.spi` → I062/080 Oktett 1 Bit 7 (`0x40`). (c) **SIM-Slot** (Oktett 2 Bit 8) dokumentiert, immer 0 (kein Simulations-Verkehr). Decoder liest MON/SPI zurück (`DecodedRecord.monosensor`/`.spi`). Kein Wire-Bruch: Multisensor-Track ohne SPI byte-identisch zu ICD 3.1.x. | verifiziert | `firefly-track`: `track::spi_is_transient_not_sticky`, `track::fresh_sensor_count_windows_per_sensor_hits`, `tracker::monosensor_flag_follows_fresh_sensor_support`, `tracker::spi_reflects_the_last_associated_report`; `firefly-asterix`: `cat062::track_status_carries_mon_and_spi_in_octet_one`, `cat062::mon_and_spi_round_trip`, `cat048::spi_bit_of_target_report_descriptor_is_decoded`; `firefly-radar`: Adapter-Durchreichung (`spi: report.spi`) |
| FR-TRK-037 | Sensor-Registrierung, Stufe REG.1 (ADR 0034, ARTAS-Roadmap AP-REG): Firefly kann die **systematischen Messfehler** eines Radars — konstanter Range-Offset (m) und Azimut-Offset (rad), `gemessen = wahr + Bias` — **offline aus Daten schätzen**. Neues Modul `firefly-track::registration` (rein, deterministisch, ohne I/O): (a) `SensorBias` (serialisierbar, ADR 0007); (b) `correspondences_by_identity` bildet „gleiches Ziel, zwei Sensoren"-Paare über die **global eindeutige Mode-S-/ICAO-Adresse** (Betreiber-Entscheid Option a) mit engem Zeitfenster (Schätzer nimmt Zeit-Ausrichtung an; systematischer Zeitversatz ist bewusst ein Folge-Häppchen); Radar↔Radar-Paare werden nur einmal gezählt; (c) `estimate_biases` löst die linearisierten Residuen-Gleichungen `d_k = J_a·b_a − J_b·b_b` (Jacobi **numerisch** per zentraler Differenz auf dem exakten Sensor→WGS84→Common-Lift, inkl. Frame-Rotation) per **SVD-Kleinste-Quadrate**; geodätische Seiten (ADS-B-Selbstreport) dienen als bias-freie Referenzwahrheit ohne Unbekannte; (d) das Singulärwert-Spektrum liefert die **Beobachtbarkeits-Diagnose** (`observable=false` bei Rangdefizit, z. B. ko-lokierte Radare ohne Referenz — Minimum-Norm-Lösung, nicht operationell anwenden) plus Residuen-RMS vor/nach. **Ehrliche Grenzen (REG.1):** offline (Live-Korrektur = REG.2), kein Zeit-Offset, ungewichtet. Kein Live-/Wire-Eingriff. | verifiziert | `firefly-track`: `registration::jacobian_matches_analytic_in_flat_geometry`, `registration::single_radar_bias_recovered_against_geodetic_truth` (injizierte 150 m/0,3° unter Rauschen zurückgewonnen), `registration::two_radar_biases_recovered_from_mutual_sightings` (beide absolut, ohne Referenz), `registration::zero_bias_yields_zero_estimates`, `registration::colocated_radars_without_reference_are_flagged_unobservable`, `registration::no_radar_correspondences_yield_none`, `registration::pairing_matches_by_identity_nearest_in_time`, `registration::pairing_respects_the_time_window`, `registration::radar_pairs_are_not_double_counted` |
| FR-TRK-038 | Sensor-Registrierung, Stufe REG.2a — **Online-Schatten-Monitor** (ADR 0034, ARTAS-Roadmap AP-REG): Der REG.1-Schätzer läuft **live im Server mit**, ohne die Fusion zu verändern. (a) `firefly-track::RegistrationMonitor` (rein, datenzeit-getrieben, ADR 0003): hält ein gleitendes Datenzeit-Fenster identitäts-tragender, registrierungs-nutzbarer Plots (Radar-Polar nur von gelisteten Sensoren, geodätische Referenz immer), paart per `correspondences_by_identity` und re-schätzt in fester Kadenz per `estimate_biases`; zu dünne Evidenz (< `min_correspondences`) wird **abgelehnt** statt verrauscht geschätzt. `RegistrationConfig`-Defaults: 120 s Fenster, 1 s Pairing, 10 s Kadenz, min. 20 Paare. (b) **Server-Verdrahtung (Schattenmodus):** `LiveTracker.with_registration` beobachtet jede Ingest-Batch **nach** der Tracker-Verarbeitung — nachweislich ohne Wirkung aufs Lagebild; Aktivierung opt-in via `FIREFLY_REGISTRATION_ENABLED` (bool-Konvention `1/true/yes`), nur sinnvoll mit ≥ 1 Radar-Quelle (sonst Warn-Log, No-op). (c) **Observability:** je frischer Schätzung ein `info`-Log (Paare, RMS vor/nach, Beobachtbarkeit, Biases je Sensor); Prometheus: `firefly_registration_estimates_total`, `firefly_registration_correspondences`, `firefly_registration_observable`, `firefly_registration_bias_range_m{sensor=…}`, `firefly_registration_bias_azimuth_deg{sensor=…}` (gelabelte Gauges, erst nach erster Schätzung). **Kein** Wire-/ICD-Bezug; die Rückkopplung in die Fusion (Anwendungs-Politik) ist REG.2b. | verifiziert | `firefly-track`: `registration::monitor_estimates_injected_bias_from_the_stream` (150 m/0,3° aus dem laufenden Strom zurückgewonnen), `registration::monitor_respects_the_estimation_cadence`, `registration::monitor_evicts_stale_plots_and_refuses_thin_evidence`; `firefly-server`: `live::registration_shadow_mode_does_not_change_the_picture` (identisches Lagebild mit/ohne Monitor), `live::registration_flag_parses_the_boolean_convention`, `metrics::render_includes_all_metrics`, `metrics::registration_bias_gauges_absent_without_estimates` |
| FR-TRK-039 | Sensor-Registrierung, Stufe REG.2b — **Bias-Korrektur vor der Fusion** (ADR 0034, ARTAS-Roadmap AP-REG): Die geschätzten Radar-Biases werden von den Live-Messungen abgezogen, abgesichert durch eine **Anwendungs-Politik**. (a) `firefly-track::RegistrationApplier` + `ApplyPolicy` (rein, deterministisch): Eine Schätzung wird nur übernommen, wenn **jedes** Gate-Kriterium erfüllt ist — `observable` (keine Minimum-Norm-Lösung aus rangdefizienter Geometrie), signifikanter Residuen-Gewinn (`rms_after ≤ 0,5 · rms_before` — sonst fittet die Schätzung Rauschen; hält bei Null-Bias die Korrektur korrekt bei Null) und plausible Größenordnung (|Δr| ≤ 1000 m, |Δθ| ≤ 1°). (b) **Geglättete Übergänge:** angewandte Korrektur = exponentieller Tiefpass (α = 0,3 je Schätzlauf) der akzeptierten Schätzungen — kein Sprung im Lagebild; Gate-Ausfälle werden `hold_runs` = 3 Läufe **gehalten** (transiente Verkehrslöcher wickeln eine gute Kalibrierung nicht ab), danach klingt die Korrektur zur Null ab. (c) **Stabilität per Konstruktion:** Der Monitor (FR-TRK-038) beobachtet weiterhin den **rohen** Strom — die Schätzung ist der *volle* Bias, unabhängig von der Anwendung; die Korrektur ist ein reiner Tiefpass ohne Integrator, nichts kann oszillieren (im Konvergenz-Test belegt: monoton, kein Überschwingen). (d) **Server-Verdrahtung:** `LiveTracker::with_registration_apply`; Korrektur (`r − Δr`, `θ − Δθ`, azimut-normalisiert) **vor** `process_plots`, nur für gelistete Radare (geodätische Plots und fremde Sensoren unberührt); Applier rückt **genau einmal je Schätzlauf** vor (`RegistrationMonitor::runs_total`, abgelehnte Läufe zählen für Hold/Decay); die `.ffplots`-Aufzeichnung enthält bewusst die **rohen** Plots (Replay durchläuft dieselbe Korrektur-Logik). Opt-in via `FIREFLY_REGISTRATION_APPLY` **zusätzlich** zu `FIREFLY_REGISTRATION_ENABLED` (Regelkreis in den Fusionspfad wird ausdrücklich geschaltet; `_APPLY` ohne Monitor → Warn-Log, No-op). (e) **Observability:** `firefly_registration_apply_active` (0/1), `firefly_registration_applied_bias_range_m{sensor=…}` / `_applied_bias_azimuth_deg{sensor=…}` (getrennt von den rohen Schätzwerten), Log bei Übernahme/Rücknahme. **Kein** Wire-/ICD-Bezug (Bias-Statistik auf den Draht = REG.3). | verifiziert | `firefly-track`: `registration::applier_gate_rejects_each_criterion_alone`, `registration::applier_smooths_toward_the_accepted_estimate`, `registration::applier_holds_then_decays_after_sustained_gate_failures`, `registration::correct_subtracts_bias_only_for_known_radar_measurements`, `registration::closed_chain_converges_to_the_injected_bias` (Monitor→Gate→Glättung→Korrektur, 150 m/0,3° monoton zurückgewonnen, korrigierte Messung < 10 m neben der Wahrheit); `firefly-server`: `live::registration_apply_corrects_the_radar_picture` (End-to-End: korrigiertes Lagebild sitzt auf der Wahrheit, unkorrigiertes trägt den 800-m-Bias), `metrics::render_includes_all_metrics`, `metrics::registration_bias_gauges_absent_without_estimates` |
| FR-TRK-029 | Track-Ende-Signalisierung (ADR 0016): `SystemTrack` trägt `ended: bool` (Default `false`). An beiden Löschstellen (`process_scan` Batch + zeit-kontinuierlicher `process_plots`) fängt `delete_and_buffer_ended` den **vollständigen letzten Zustand** jedes gelöschten Tracks als `SystemTrack` mit `ended = true` in einen Puffer ein, bevor er aus dem Live-Set fällt. `Tracker::take_ended_tracks()` leert den Puffer für die Ausgabe-Stufe (T3); ein gelöschter Track wird damit **genau einmal** als Ende-Record gemeldet und danach nie wieder. Lebende Tracks tragen stets `ended = false`. Grundlage für CAT062 I062/080 TSE (FR-IO-003). Die Ausgabe-Stufe (`Player::periodic_snapshots`, der CAT062/Multicast-Pfad) hängt die per `take_ended_tracks` gedrainten Ende-Records einmalig an den jeweiligen Tick an; der JSON/Web-Pfad (`periodic_frames`) bleibt unberührt (sein `FrameTrack` kennt kein Track-Ende). Ehrliche Grenze (ADR 0016): im async-Pfad treibt eintreffender Plot-Verkehr die Löschung — bei komplett stillem Feed wird (noch) kein TSE gesendet. | verifiziert (T2+T3) | `firefly-track`: `tracker::live_track_is_never_ended`, `tracker::deleted_track_is_buffered_once_with_ended_flag`; `firefly-player`: `deleted_track_is_reported_once_as_ended_in_periodic_snapshots` |
| FR-IO-001 | Der Tracker-Ausgabestrom wird zu einem neutralen `Frame` (Datenzeit + Sensor + System-Tracks) gebündelt und verlustfrei nach JSON serialisiert/deserialisiert; erster Ausgabe-Adapter (ADR 0009). Die Wire-Form ist web-freundlich (Position in Grad, abgeleitete Geschwindigkeit/Kurs) und vom internen `SystemTrack`-Layout entkoppelt (Ports & Adapters). Der JSON-Pfad **darf** (anders als der CAT062-Draht-Vertrag) die abgeleitete Provenienz explizit führen: `FrameTrack` trägt `provenance` (`"psr"`/`"ssr"`/`"mode_s"`/`"adsb"`/`"flarm"`/`"combined"`/`"unknown"`) und die rohen `source_ages` (ADR 0027, FR-TRK-034) — das ASD-Showcase rendert daraus die Herkunfts-Symbolik direkt. | verifiziert | `firefly-io`: `frame::wire_form_uses_degrees_and_derived_kinematics`, `frame::frame_round_trips_through_json`, `frame::json_is_self_describing`, `frame::empty_frame_has_no_tracks` |
| FR-IO-002 | Der „Player" führt ein Szenario durch den Tracker und erzeugt daraus den vollständigen, zeitlich geordneten Frame-Strom (ein `Frame` je Scan-Zeit) — als reine, deterministische Funktion ohne Netz/Wanduhr (Grundlage für Server- und Demo-Tempo in 3.3/3.5). | verifiziert | `firefly-player`: `one_frame_per_scan_time_in_order`, `confirmed_track_appears_in_frame_stream`, `frame_stream_is_deterministic`, `scenario_without_radar_yields_no_frames` |
| FR-IO-003 | Der Tracker-Ausgabestrom wird zusätzlich als **ASTERIX CAT062** (binäre System-Tracks) kodiert — der Andock-Kontrakt ans ASD (ADR 0006). Eigene Crate `firefly-asterix`, neben dem JSON-Adapter, auf demselben neutralen `SystemTrack`; bit-genaues Framing (`[CAT][LEN][Record]`) mit FSPEC/UAP-Mechanik. Felder: I062/010, /070, /105, /100, /185, /040, /080, /290, /500; **Identität** (nur bei vorhandener SSR-Identität, FR-TRK-009): I062/060 (Mode 3/A), I062/245 (Target Identification / Callsign, FRN 10, FR-TRK-028) und I062/380/ADR (Mode-S-24-Bit-Adresse als Target-Address-Subfeld); **Vertikallage** (nur bei vorhandener Mode-C-Flugfläche, FR-TRK-027): I062/136 (Measured Flight Level, FRN 17, signed i16, LSB 1/4 FL); **Track-Ende** (nur bei gelöschtem Track, FR-TRK-029): I062/080 TSE-Bit (Oktett 2, Bit 7, ADR 0016); **ADS-B-Alter** (nur bei vorhandenem ADS-B-Treffer, FR-TRK-032): I062/290 ES-Age-Subfeld (Bit `0x08` im primären Subfeld-Oktett, ICD 2.4.0, AP9.5); **Per-Technologie-Alter** (nur bei vorhandenem Treffer der jeweiligen Technologie, FR-TRK-034, ADR 0027): I062/290 SSR-Age (`0x20`), Mode-S-Age (`0x10`) und FLARM-Age (`0x04`, Firefly-Vendor-Subfeld) — Age-Oktette in Bit-Priorität MSB→LSB (PSR→SSR→MDS→ES→FLARM), je u8, LSB 0,25 s; der Decoder liest sie positionsbasiert und füllt `DecodedRecord.source_ages`. Länge des I062/290-Items ist variabel (1 primäres Subfeld-Byte + 1 Byte je gesetztem Subfeld-Bit). Die FRNs folgen der echten EUROCONTROL-UAP — I062/500 auf FRN 27 (nicht 16), I062/295 (FRN 16) reserviert/ungenutzt (ADR 0015). I062/100 (System-Stereografisch, X/Y) wird **zusätzlich** zu I062/105 (WGS84) aus der WGS84-Position über `StereographicProjection` (FR-GEO-004, Referenzpunkt = Systemreferenzpunkt) berechnet (ADR 0006-Nachtrag, Häppchen C.2). | fertig (3.X.1–3.X.4 + 4.2 + C.2 + AP7 + T4 + AP9.5; LSB-/Subfeld-Werte gegen EUROCONTROL SUR.ET1.ST05.2000-STD-09-01 Ed. 1.10 verifiziert; Meilenstein-Doku `docs/milestones/M3X-cat062-encoder.md`) | `firefly-asterix`: `fspec::*`, `cat062::single_track_matches_reference_dump`, `cat062::time_of_track_scales_to_128th_seconds`, `cat062::position_scales_to_wgs84_lsb_and_signs_via_twos_complement`, `cat062::cartesian_component_scales_to_half_metre_and_signs_via_twos_complement`, `cat062::position_cartesian_uses_system_stereographic_projection`, `cat062::velocity_scales_to_quarter_mps_and_signs_via_twos_complement`, `cat062::track_status_carries_cnf_and_extends_for_cst`, `cat062::track_status_carries_tse_when_ended`, `cat062::update_ages_use_psr_subfield_in_quarter_seconds`, `cat062::update_ages_appends_es_subfield_when_adsb_age_present`, `cat062::update_ages_es_round_trip`, `cat062::update_ages_no_es_subfield_when_adsb_age_absent`, `cat062::update_ages_appends_per_technology_subfields_in_priority_order`, `cat062::update_ages_per_technology_round_trip`, `cat062::single_track_with_adsb_hit_matches_reference_dump`, `cat062::decode_recovers_adsb_age_when_present`, `cat062::accuracies_use_apc_subfield_in_half_metres`, `cat062::mode_3a_carries_the_octal_reply_in_low_twelve_bits`, `cat062::target_address_uses_adr_subfield_with_24_bit_address`, `cat062::identity_items_appear_only_when_present`, `cat062::length_field_covers_all_records`, `cat062::flight_level_scales_to_quarter_fl_and_signs_via_twos_complement`, `cat062::target_identification_packs_eight_six_bit_ia5_codes` |
| FR-NET-001 | Ein Web-Server (axum/Tokio, ADR 0009) stellt den Frame-Strom über eine **WebSocket**-Verbindung als JSON bereit und bietet Health-/Readiness-Probes (ADR 0003). Die Zustellung wird am **Ausgabe-Rand** nach Datenzeit getaktet (Tempo-Faktor), ohne den datenzeit-getriebenen Kern zu berühren. | verifiziert | `firefly-server`: `websocket::websocket_streams_parseable_frames_in_order`, `app::health_probe_is_ok`, `app::ready_probe_is_ok`, `app::index_page_is_served`, `pacing::*`, `config::*` |
| FR-UI-001 | Das Web-Frontend stellt die Tracks auf einer 2D-Karte (**MapLibre GL**, ADR 0009) dar und konsumiert den `/ws`-Frame-Strom. Der safety-relevante Status ist *sichtbar* (ADR 0008): Farbe nach confirmed/tentativ/coasting, Positions-Unsicherheits-Ring (gestrichelt beim Coasting), Geschwindigkeitsvektor aus Kurs/Speed. | umgesetzt (Sichtprüfung manuell) | `firefly-server`: `app::index_html_is_the_maplibre_frontend` (Asset-Inhalt) + `static/index.html`; visuelle Bestätigung im Browser ist manueller Schritt |
| FR-IO-004 | Ein CAT062-Datenblock kann **dekodiert** werden — die Umkehrung von FR-IO-003: FSPEC parsen, die enthaltenen FRNs in UAP-Reihenfolge lesen und jedes Item in seinen Ursprungswert zurückrechnen (Data Source, Time of Track, WGS84-Position, system-stereografisches X/Y in Metern, Geschwindigkeit, Track-Nummer, Status inkl. Track-Ende (TSE), Update-Alter, Genauigkeit, optional Identität inkl. Callsign). Grundlage für eine Empfänger-/Recorder-Seite (Häppchen D), die das gesendete Signal unabhängig verifiziert. I062/100 (X/Y in Metern) kann mit `unproject_cartesian_position` über `StereographicProjection::unproject` (FR-GEO-004, Referenzpunkt = Systemreferenzpunkt) zurück nach WGS84 projiziert werden — stimmt mit der unabhängig kodierten I062/105-Position auf unter 1 m überein (D.2). | fertig (D.1–D.2 + AP7 + T4) | `firefly-asterix`: `fspec::parse_inverts_fspec`, `fspec::parse_of_empty_input_consumes_nothing`, `cat062::decode_inverts_encode_for_a_plain_track`, `cat062::decode_recovers_identity_when_present`, `cat062::decode_recovers_flight_level_when_present`, `cat062::decode_recovers_callsign_when_present`, `cat062::decode_recovers_status_for_tentative_coasting_track`, `cat062::decode_recovers_track_end_when_present`, `cat062::decode_handles_multiple_records`, `cat062::decode_handles_empty_block`, `cat062::decode_rejects_malformed_input`, `cat062::cartesian_origin_unprojects_to_reference_point`, `cat062::position_cartesian_unprojects_close_to_position_wgs84` |
| FR-IO-005 | Entkoppelter periodischer Ausgabetakt im Player (ADR 0013, Häppchen 13.4): `Player::periodic_snapshots(t_out)` und `periodic_frames(t_out)` arbeiten die Plots in **Datenzeit-Reihenfolge einzeln** über `Tracker::process_plot` ein und reporten das Lagebild in einem **festen Takt** `t_out` über `Tracker::snapshot_at` — unabhängig von der unregelmäßigen Eingangs-Kadenz (der stabile Herzschlag, den ein reales SDPS aussendet). Ticks ohne frische Plots liefern weiterhin einen Snapshot (coastende Tracks). Default-`t_out` = kleinste Sensor-Scan-Periode der Szene (`Player::default_output_period`), per `FIREFLY_OUTPUT_PERIOD` überschreibbar (Server-Anbindung beim Cutover, Häppchen 13.7). `periodic_frames` bündelt zusätzlich die Roh-Plots des jeweiligen Ausgabe-Fensters (Plot-Overlay-Parität). Reine, deterministische Funktion (NFR-CLOUD-002, NFR-REPRO-001). Bestehende `scans()`/`frames()` (Batch) bleiben unverändert (Ansatz B). Hinweis: bis zum async-Simulator (Häppchen 13.5) für Einzel-Radar validiert; Multi-Radar-Async-Regression in 13.6. | verifiziert | `firefly-player`: `default_output_period_is_the_fastest_scan_period`, `periodic_snapshots_emit_on_a_fixed_heartbeat`, `periodic_snapshots_confirm_a_track`, `finer_heartbeat_emits_more_ticks_than_input_scans`, `periodic_frames_are_deterministic`, `periodic_frames_bundle_window_plots_and_tracks` |
| FR-IO-005 | Ein **ASTERIX-CAT048-Datenblock** (Monoradar Target Reports, EUROCONTROL SUR.ET1.ST05.2000-STD-04-01) kann **dekodiert** werden — der Eingangs-Gegenpart zu FR-IO-003 (ADR 0028). Eigenes Modul `firefly-asterix::cat048` (gemeinsame `fspec`-Mechanik): `decode_target_reports` liest je Record das FSPEC und decodiert die **plot-relevanten** Items (I048/010 SAC/SIC, I048/140 ToD, I048/020 TYP→`Detection`, I048/040 Polar RHO 1/256 NM→m + THETA 360/2¹⁶°→rad, I048/070 Mode-3/A, I048/090 Mode-C FL signed 14-Bit/¼-FL, I048/220 ICAO, I048/240 Callsign, I048/161 Track-Nummer); alle übrigen Standard-Items werden **längen-korrekt übersprungen** über ein vollständiges Pro-FRN-Format-Modell (fixed/extended/repetitive/compound/explicit). **Robustheit (untrusted-Eingangspfad, Charta §8):** jede Lesung läuft über einen grenzen-geprüften `Cursor`, ein truncierter/korrupter Record liefert `Cat048DecodeError` (Datagramm verworfen, kein Panic), ein present-aber-unbekannter FRN ist ein harter Fehler statt stiller Fehl-Parse. TYP→`Detection`→`DetectionKind`/`SourceKind` speist die Provenienz (ADR 0027) aus echtem Radar. | verifiziert | `firefly-asterix`: `cat048::reference_block_decodes_all_fields`, `cat048::target_report_descriptor_typ_maps_to_detection`, `cat048::flight_level_decodes_negative_via_twos_complement`, `cat048::skips_unused_standard_items_length_correctly`, `cat048::wrong_category_is_rejected`, `cat048::length_mismatch_is_rejected`, `cat048::every_standard_frn_has_a_format`, `cat048::truncations_never_panic`, `cat048::single_byte_mutations_never_panic` |
| FR-NET-002 | Der CAT062-Strom wird zusätzlich als **UDP-Multicast** versendet (ADR 0006-Nachtrag, ED-109A-Stil): je Scan ein CAT062-Datenblock an eine Multicast-Gruppe; Konsumenten (ASD, EFS, Recorder) hören unabhängig mit, der Sender kennt sie nicht (Entkopplung, ADR 0003). Eigene Crate `firefly-multicast`, 12-Factor-Konfiguration (`FIREFLY_CAT062_*`: Gruppe, Port, SAC/SIC), per Default **aus**. Der I062/100-Referenzpunkt ist seit ADR 0021 der System-Referenzpunkt (FR-GEO-005), nicht mehr eine Multicast-eigene Variable. Die Zustellung wird am Ausgabe-Rand nach Datenzeit getaktet, ohne den datenzeit-getriebenen Kern zu berühren. Beide Ausgabe-Adapter (Web/JSON, CAT062/Multicast) übersetzen denselben `SystemTrack`-Strom (`Player::scans`) unabhängig voneinander (M3.X.4). Das Modul `receiver` ist die Konsumenten-Seite (FR-IO-004, Häppchen D.3): `receiver_socket` tritt der Multicast-Gruppe bei, `recv_records`/`run` dekodieren die Datenblöcke per `decode_data_block` zurück in `DecodedRecord`s — der Gegenbeweis, dass Sender und Decoder zusammen ein nutzbares Signal ergeben. | verifiziert | `firefly-multicast`: `sender::sends_one_cat062_block_per_scan`, `sender::empty_scan_list_sends_nothing`, `receiver::receiver_decodes_what_sender_sends`, `receiver::receiver_socket_joins_the_multicast_group`, `pacing::*`, `config::*`; `firefly-player`: `scans_and_frames_share_the_same_scan_times` |
| FR-IO-006 | Der Multicast-Strom trägt zusätzlich einen **ASTERIX-CAT065-SDPS-Service-Status (Heartbeat)** — das Lebenssignal des Datenverarbeitungssystems, damit ein Konsument „leerer Himmel" von „toter Feed" unterscheiden kann (ADR 0018). `firefly-asterix::cat065` kodiert eine periodische SDPS-Status-Meldung (I065/000 = 1) mit I065/010 (SAC/SIC), I065/015 (Service Identification), I065/030 (Time of Day, 1/128 s wie I062/070, **wall-clock**-Aussendezeit) und I065/040 (NOGO operationell/degradiert), und dekodiert sie wieder (`decode_status_block`). FRNs folgen der echten EUROCONTROL-CAT065-UAP (SUR.ET1.ST05.2000-STD-13-01); byte-genauer Referenz-Dump. Gemeinsamer Strom mit CAT062, Dispatch am CAT-Oktett (`0x3E`/`0x41`), ICD → 2.3.0 additiv. | fertig (Encoder + Decoder, byte-genau gegen Referenz; Doku ADR 0018, ICD §8) | `firefly-asterix`: `cat065::status_matches_reference_dump`, `cat065::time_of_day_is_encoded_in_128th_seconds`, `cat065::degraded_status_sets_nogo`, `cat065::round_trip_recovers_status`, `cat065::wrong_category_is_rejected`, `cat065::truncated_input_is_rejected` |
| FR-NET-003 | Der CAT065-Heartbeat wird als **UDP-Multicast** auf **dieselbe** Gruppe/Port wie CAT062 gesendet (ADR 0018), als eigener, entkoppelter Sende-Task im **wall-clock-Takt** (Default 1 s) — ein Echtzeit-Lebenssignal, das den datenzeit-getriebenen Track-Pfad und den Tracker-Kern nicht berührt. `firefly-multicast::run_heartbeat`; 12-Factor-Konfiguration (`FIREFLY_CAT065_ENABLED` Default an, `FIREFLY_CAT065_PERIOD`, `FIREFLY_CAT065_SERVICE_ID`), greift nur wenn der Feed (`FIREFLY_CAT062_ENABLED`) läuft. `firefly-server` startet den Task (`spawn_cat065_heartbeat`), stempelt die UTC-Tageszeit ein und zählt `firefly_cat065_heartbeats_sent_total` (NFR-OBS-001). | verifiziert | `firefly-multicast`: `heartbeat::heartbeats_are_sent_and_decodable`, `config::heartbeat_config_is_parsed_with_safe_defaults`; `firefly-server`: `metrics::render_includes_all_metrics` |
| FR-NET-004 | Der Firefly-Tracker empfängt **ADS-B-Positionsmeldungen von der OpenSky-Network-REST-API** und fügt sie als `Plot::adsb` (Measurement::Geodetic, WGS84 + isotrope Kovarianz) in denselben Tracking-Kanal ein wie Radarmessungen (ADR 0019). Eigene Crate `firefly-opensky` (Ports & Adapters-Adapter, ADR 0001): `OpenSkyConfig` liest die 12-Factor-Konfiguration (`FIREFLY_OPENSKY_ENABLED/LAT_*/LON_*/POLL_INTERVAL_SECS/CLIENT_ID/CLIENT_SECRET/TOKEN_URL/SENSOR_ID`; OAuth2 Client-Credentials seit ADR 0024) aus Umgebungsvariablen über `from_lookup`; `parse_state` parst einen Zustandsvektor (heterogenes JSON-Array, 17 Felder), wendet die NaCp→σ-Tabelle an (src=0→75 m, src=1/2→200 m, Default→300 m, ADR 0019 Tabelle) und gibt `None` für am-Boden-Ziele oder fehlende Koordinaten zurück; `OpenSkyPoller` baut einen persistenten `reqwest`-Client (rustls-tls, 30 s Timeout), holt bei gesetzten Credentials ein OAuth2-Bearer-Token (gecacht, 401-Recovery, ADR 0024) und pollt periodisch (Default 10 s, für anonymen Zugang; authentifizierter Zugang erlaubt ~5 s), konvertiert die Antwort in `Vec<Plot>` und ruft einen Callback auf; transiente Poll-Fehler werden geloggt ohne die Schleife zu unterbrechen. **429-Backoff (Wayfinder #49, ADR-0029-Folge):** ein `HTTP 429` (Rate-Limit) wird als eigener `PollError::RateLimited` erkannt (vor `error_for_status`), distinkt geloggt und in `firefly_opensky_rate_limited_total` gezählt; die Schleife **dehnt bei jedem Fehler das Poll-Intervall exponentiell** (`Backoff`: 2·base, 4·base … gedeckelt bei 300 s bzw. ≥ base) und **springt bei erstem Erfolg auf den Basistakt zurück** — statt einen rate-limitierten/toten Feed weiter im festen Takt zu hämmern. Der Adapter ist per Default **aus** (`enabled: false`) und erzeugt keinerlei Netzverkehr, solange er nicht explizit aktiviert wird. | verifiziert (AP9.4; OAuth2 ADR 0024; 429-Backoff #49) | `firefly-opensky`: `config::empty_environment_yields_defaults`, `config::valid_values_are_parsed`, `config::invalid_values_fall_back_to_defaults`, `config::enabled_flag_accepts_common_spellings`, `config::token_url_defaults_and_overrides`; `poller::backoff_first_failure_doubles_the_base`, `poller::backoff_doubles_on_each_consecutive_failure`, `poller::backoff_is_capped_at_the_maximum`, `poller::backoff_resets_to_base_on_success`, `poller::backoff_cap_is_never_below_the_base_interval`, `poller::backoff_floors_a_zero_base_at_one_second`, `poller::rate_limited_error_is_flagged`; `auth::reuses_cached_token_until_due`, `auth::refetches_after_invalidate`, `auth::fetch_error_propagates`, `auth::needs_refresh_respects_skew`; `api::well_formed_state_produces_plot`, `api::on_ground_state_is_rejected`, `api::missing_position_is_rejected`, `api::invalid_icao24_is_rejected`, `api::sigma_from_source_follows_adr0019_table`, `api::empty_callsign_produces_none` |
| FR-OPS-005 | Der CAT062/CAT065-Multicast-Feed kann **aufgezeichnet und bit-genau wiedergegeben** werden (SDPS-005 Legal Recording & Replay). `firefly-record` (Sidecar) tritt derselben Multicast-Gruppe wie jeder andere Konsument bei und schreibt jedes eingehende Datagramm mit Unix-Nanosekunden-Zeitstempel in eine `.ffrec`-Datei (Binärformat: 16-Byte-Header + Datensätze `[u64 ts_ns][u16 len][payload]`). `firefly-replay` liest diese Datei und sendet die Datagramme mit dem originalen Inter-Paket-Timing auf dieselbe Gruppe zurück — wahlweise skaliert durch `FIREFLY_REPLAY_SPEED`. Weil Firefly deterministisch nach Datenzeit arbeitet, ist das resultierende Feed für alle Konsumenten bit-identisch zum Original. Keine Eingriffe in den Firefly-Kern; keine Schnittstellen-Änderungen. 12-Factor-Konfiguration (`FIREFLY_CAT062_GROUP`/`PORT`, `FIREFLY_RECORD_OUTPUT`, `FIREFLY_REPLAY_INPUT`, `FIREFLY_REPLAY_SPEED`). | verifiziert | `firefly-recorder`: `header_round_trip`, `bad_magic_is_rejected`, `unsupported_version_is_rejected`, `record_round_trip`, `clean_eof_returns_none`, `full_file_round_trip` |
| FR-OPS-006 | Der **Eingangs-Plot-Strom** (alle Sensoren → Tracker) kann als `.ffplots`-Datei **aufgezeichnet und deterministisch wiedergegeben** werden, sodass jeder Produktions-Lauf reproduzierbar ist und Tracker-Fehler aus dem Betrieb nachstellbar werden (ADR 0020, Eingangs-Aufzeichnungsebene komplementär zu FR-OPS-005). Das `.ffplots`-Format teilt das Record-Framing von `.ffrec` (`[u64 ts_ns][u16 len][payload]`), nutzt aber das Magic `FFPLOTS\x00` und serialisiert je Record einen `Plot` als JSON (`serde_json`). **Quellenagnostisch:** ADS-B, PSR/SSR und FLARM serialisieren über denselben `Plot`-Typ — keine quellenspezifische Replay-Logik. `firefly-core::Plot` (+ `DetectionKind`/`Measurement`/`ModeAC`) leiten dafür `Serialize`/`Deserialize` ab. Robuster Reader: fehlerhafte JSON-Payloads werden als `ReadError::PlotDeserialize` gemeldet statt zu paniken. Für **bit-genaue** Wiedergabe nutzt `serde_json` das `float_roundtrip`-Feature (der Parser gewinnt f64 bit-genau zurück, nicht nur der ryu-Serializer). (AP9.4c-0.) **Live-Verdrahtung (QW.4, ARTAS-Roadmap):** Der `PlotRecorder` ist im Live-Server verdrahtet — `main.rs` löst über `resolve_plot_recorder` die **opt-in**-Env `FIREFLY_PLOT_RECORD_PATH` auf und übergibt bei gesetztem, öffenbarem Pfad einen Recorder an `LiveTracker::new`; unset/leer → kein Recording (Default), nicht-öffenbarer Pfad → Warn-Log + kein Recording (nicht-fatal, Verfügbarkeit vor Aufzeichnung). Damit ist der `.ffplots`-Wiederanlauf-/Replay-Pfad im echten Betrieb nutzbar (Vorstufe SDPS-002/HA). | verifiziert | `firefly-recorder`: `plot_header_round_trip`, `plot_header_rejects_ffrec_magic`, `plot_record_round_trip_preserves_plot`, `plot_full_file_round_trip_mixed_sources`, `plot_clean_eof_returns_none`, `plot_malformed_json_is_rejected`; `firefly-server`: `live::plot_recorder_resolves_opt_in_path`, `live::plot_recorder_unwritable_path_is_non_fatal`, `live::recorder_captures_every_ingested_plot` |
| FR-OPS-007 | Im **Live-Modus** (ADR 0020) läuft ein langlebiger `Tracker` als Tokio-Task (`firefly-server::live::run_live_tracker`), gefüttert über einen `mpsc`-Kanal von den Sensor-Adaptern (OpenSky-ADS-B heute; PSR/SSR, FLARM später). Der Task verarbeitet jeden Plot-Batch **nach Datenzeit** (`Tracker::process_plots`, ADR 0013 — der Kern bleibt deterministisch, nur die *Ankunft* ist wanduhr-getrieben) und veröffentlicht nach jedem Ausgabetakt einen frischen `Vec<SystemTrack>`-Snapshot über `tokio::sync::watch`, sodass WS-Pump und CAT062-Feed den jeweils neuesten Snapshot lesen, ohne den Tracker zu blockieren. Ist die opt-in-Aufzeichnung aktiv (`FIREFLY_PLOT_RECORD_PATH`, FR-OPS-006/QW.4), wird **jeder eingehende Plot vor dem Tracker** über den `PlotRecorder` in eine `.ffplots`-Datei geschrieben — Reproduzierbarkeit trotz nicht-deterministischer Ankunft (NFR-REPRO-001). Schreibfehler der Aufzeichnung deaktivieren nur den Recorder, nicht das Tracking (Verfügbarkeit vor Aufzeichnung). Der Tracking-Frame liegt im Mittelpunkt der konfigurierten OpenSky-Bounding-Box. | umgesetzt (AP9.4c-2 + AP9.4c-3) | `firefly-server`: `live::snapshot_is_empty_before_any_plot`, `live::repeated_hits_confirm_one_track`, `live::two_aircraft_make_two_tracks`, `live::recorder_captures_every_ingested_plot`, `live::task_publishes_snapshot_then_stops_on_close` |
| FR-NET-007 | Der **WebSocket-Frame-Pump im Live-Modus** (`firefly-server::app::pump_live`) liest kontinuierlich aus dem `watch::Receiver<LiveSnapshot>` und sendet je empfangenem Snapshot einen neuen `Frame` (gebündelt mit Timestamp und Sensor-ID) an den verbundenen WebSocket-Client als JSON. Der `LiveSnapshot` trägt die Datenzeit, damit der Frame-Ausgangs-Adapter `frame.time` korrekt stempelt — beide Output-Ports (JSON/WS und CAT062/Multicast) verwenden dieselbe Quelle und liefern konsistent zeitgestempelte Daten. Der Pump entkoppelt vom Tracker durch das Watch-Channel (blockierfrei, letzter-Wert-Semantik). | umgesetzt (AP9.4c-3) | `firefly-server`: `app::pump_live` (Implementierung in `app.rs`), HTTP `GET /`-Endpoint ruft `pump_frames` auf, die `FrameSource::Live { snapshots, sensor }` dispatcht; im Test: `app_test::websocket_client_receives_frames_in_live_mode` |
| FR-NET-008 | Der **CAT062-UDP-Multicast-Sender im Live-Modus** (`firefly-server::live::run_live_cat062`) liest kontinuierlich aus dem `watch::Receiver<LiveSnapshot>` und enkodiert je empfangenem Snapshot ein neues CAT062-Datenblock-Paar (Track-Records + optionale Ende-Records). Die Snapshot-Datenzeit steuert den Encoder direkt als `time` für die `I062/070`-Kodierung. Der Sender hat den Datagram-Encode-Fehler-Callback für Metriken (`on_scan`). Seit ADR 0030 ist dies der einzige CAT062-Sendepfad des Servers. | umgesetzt (AP9.4c-3) | `firefly-server`: `live::run_live_cat062` (Implementierung in `live.rs`), `main.rs` ruft `spawn_cat062_live` auf; im Test: `live_test::live_cat062_sender_encodes_and_sends_snapshot` |
| FR-NET-009 | Die **Readiness-Probe `/ready`** (Kubernetes) antwortet `503 not ready` bis zum ersten Plot einer konfigurierten Quelle, danach `200 ready` (ADR 0020, AP9.4c-4) — Kubernetes sendet keinen Traffic an einen Pod ohne Luftlagebild. **Ausnahme (ADR 0030):** Eine Instanz **ohne** konfigurierte Quellen ist sofort ready — ihr leerer Himmel ist das vollständige Bild; `main` setzt `metrics.live_ready` beim Start. Sonst setzen die Quell-Adapter (OpenSky/FLARM/Radar) das Flag beim ersten Plot. | umgesetzt (AP9.4c-4) | `firefly-server::app`: `ready` Handler (`app.rs`), zwei neue Tests `ready_probe_returns_503_in_live_mode_before_first_poll`, `ready_probe_returns_ok_in_live_mode_after_first_poll` |
| FR-OBS-003 | **Live-Modus Metriken** (AP9.4c-4): Prometheus exponiert Counter/Gauges für den Live-Tracker: `firefly_live_plots_ingested_total` (Plots an Tracker), `firefly_plot_records_written_total` (`.ffplots`-Aufzeichnung), `firefly_opensky_poll_errors_total` (HTTP/Netz-Fehler), `firefly_opensky_rate_limited_total` (HTTP-429-Teilmenge, treibt den 429-Backoff, #49), `firefly_flarm_plots_received_total` (FLARM/OGN-Plots, ADR 0026). Das Flag `metrics.live_ready: AtomicBool` (intern, nicht im `/metrics`-Output) steuert die `/ready`-Probe. Der Live-Tracker-Task ruft einen `on_tick(plots, records)`-Callback nach jedem Output-Takt auf (ADR 0020); der OpenSky-Poller-Task ruft `on_error(PollError)` auf bei jedem fehlgeschlagenen Poll. | umgesetzt (AP9.4c-4) | `firefly-server`: `metrics::render` und `Metrics` Struktur (metrics.rs), Live-Tracker-Task `run_live_tracker` (live.rs), OpenSky-Poller `OpenSkyPoller::run` (firefly-opensky, poller.rs), Tests `metrics::render_includes_all_metrics` |
| FR-IO-007 | Der Multicast-Strom trägt zusätzlich **ASTERIX-CAT063-Sensor-Status-Meldungen** — der Per-Sensor-Liveness-Bericht des SDPS, damit ein Konsument einen ausgefallenen Sensor von einem leeren Himmel unterscheiden kann (ADR 0022, Firefly #32; **UAP-Standardisierung ADR 0032**). `firefly-asterix::cat063` kodiert einen Block mit **einem Record je registriertem Sensor** nach der **echten EUROCONTROL-CAT063-UAP** (FSPEC `0xB8`, FRN 1+3+4+5): I063/010 (SAC/SIC des **SDPS**, dieselbe Identität wie I062/010), I063/030 (Time of Day, 1/128 s wie I062/070, **wall-clock**-Aussendezeit), I063/050 (SAC/SIC des **Sensors**, über den der Record berichtet) und I063/060 (CON operationell `0x00` / degradiert `0x40`, variabel via FX), und dekodiert sie wieder (`decode_sensor_block`, robust gegen falsche Kategorie und abgeschnittene Eingabe). FRNs folgen SUR.ET1.ST05.2000-STD-04-01 (verifiziert gg. CroatiaControl ed. 1.3); byte-genauer Referenz-Dump. Gemeinsamer Strom mit CAT062/CAT065, Dispatch am CAT-Oktett (`0x3E`/`0x41`/`0x3F`), ICD → 3.0.0 **breaking** (I063/010 = SDPS, Sensor jetzt in I063/050). **Per-Quelle-Fehlergrund (ADR 0033, ICD 3.1.0, additiv):** ein **degradierter** Sensor mit bekanntem Grund trägt zusätzlich das **I063/RE** (Reserved Expansion Field, FRN 13, FSPEC dann `0xB9 0x04`) mit Vendor-Subfeld **`SRC-REASON`** (`SensorReason`: `1=unreachable`/`2=auth`/`3=rate_limited`; `0=ok` nie gesendet), Layout `[LEN=0x03][0x80][code]`. Nur bei Degradierung mit Grund gesendet (operationelle Records unverändert); `record_failure` im `SensorHealthMonitor` speist den Grund, `record_activity` setzt ihn zurück. Grund-Klassifikation in den HTTP-Pollern (`PollError::is_rate_limited`/`is_auth`); FLARM/Radar ohne Grund. | verifiziert | `firefly-asterix`: `cat063::single_operational_sensor_matches_reference_dump`, `cat063::single_degraded_sensor_matches_reference_dump`, `cat063::two_sensors_in_one_block_matches_reference_dump`, `cat063::time_of_day_is_encoded_in_128th_seconds`, `cat063::round_trip_recovers_all_fields`, `cat063::empty_sensor_list_produces_minimal_block`, `cat063::fspec_uses_standard_uap_positions`, `cat063::extended_i063_060_is_skipped_via_fx`, `cat063::degraded_sensor_with_reason_appends_re_field`, `cat063::re_reason_round_trips`, `cat063::operational_sensor_never_emits_re`, `cat063::degraded_without_reason_emits_no_re`, `cat063::unknown_reason_code_maps_to_unreachable`, `cat063::wrong_category_is_rejected`, `cat063::truncated_input_is_safe`; `firefly-multicast`: `sensor_health::recorded_failure_becomes_degraded_reason`, `sensor_health::active_sensor_reports_ok_reason`, `sensor_health::success_clears_failure_reason` |
| FR-IO-008 | Der CAT063-Sensor-Status trägt die **Registrierungs-Bias-Statistik auf dem Draht** (REG.3, ADR 0034; ICD 3.3.0, additiv): Bei **aktiver** Registrierungs-Korrektur (REG.2b, FR-TRK-039) trägt der Record eines Radar-Sensors die **angewandte** Korrektur — nicht den rohen Schätzwert — in den EUROCONTROL-Standard-Items **I063/080** (FRN 7; `SRG` Range Gain, 16-Bit-Zweierkomplement, LSB 10⁻⁵ — Firefly schätzt keinen Gain, sendet immer 0; `SRB` Range Bias, 16-Bit-Zweierkomplement, LSB **1/128 NM ≈ 14,47 m**) und **I063/081** (FRN 8; `SAB` Azimuth Bias, 16-Bit-Zweierkomplement, LSB **360/2¹⁶ ° ≈ 0,0055°**), Sättigung statt Wrap an den i16-Grenzen. **Sende-Regel:** Items nur bei in Kraft befindlicher Korrektur — **Absenz = „keine Korrektur"**, eine gesendete 0 würde fälschlich „Bias exakt Null" behaupten; ohne Korrektur ist der Record byte-identisch zur Vor-REG.3-Form (kein Wire-Bruch). FSPEC wächst dann auf `0xBB 0x80` (Record 16 Oktette). **Datenfluss:** `LiveTracker`-Tick → `Metrics.registration_applied_biases` → Bias-Provider-Closure des `run_cat063_sender` (einmal je Sendetakt abgefragt) → `SensorReport.ssr_bias`. Decoder (`decode_sensor_block`) liest beide Items einzeln FSPEC-getrieben zurück (`DecodedSensorStatus.ssr_bias`). Wayfinder-Nachzug additiv ohne Lockstep (`from-firefly`-Issue). | verifiziert | `firefly-asterix`: `cat063::sensor_with_bias_matches_reference_dump` (byte-genau, FSPEC `0xBB 0x80`), `cat063::bias_round_trips_within_lsb` (inkl. negativer Werte), `cat063::no_bias_emits_no_bias_items`, `cat063::mixed_bias_and_plain_records_decode`, `cat063::out_of_range_bias_saturates`; `firefly-multicast`: `cat063_sender::applied_bias_reaches_the_wire` (End-to-End über UDP), `cat063_sender::cat063_blocks_are_sent_and_decodable` (ohne Registrierung keine Bias-Items) |
| FR-IO-009 | Ein **ASTERIX-CAT034-Datenblock** (Monoradar Service Messages, SUR.ET1.ST05.2000-STD-02b) kann **dekodiert** werden — das Service-Gegenstück zum CAT048-Zielmeldungs-Decoder (FR-IO-005) auf demselben Radar-Feed (FEP.1, ARTAS-Roadmap AP-FEP). Neues Modul `firefly-asterix::cat034` (gemeinsame `fspec`-Mechanik): `decode_service_messages` liest je Record die **service-relevanten** Items — I034/010 (SAC/SIC), I034/000 (Message Type: 1 = **Nordmarke**, 2 = **Sektor-Meldung**, 3/4 = Geo-Filter/Jamming, unbekannte Codes tolerant als `Other`), I034/030 (Time of Day, 1/128 s), I034/020 (Sektornummer, LSB 360/2⁸ °), I034/041 (gemeldete Antennen-Umlaufzeit, LSB 1/128 s) — und **überspringt alle übrigen Standard-Items längen-korrekt** über ein vollständiges Pro-FRN-Format-Modell (fixed/repetitive/compound/explicit; die Compound-Items I034/050/060 über Pro-Position-Subfeld-Längentabellen, ein gesetztes **Spare**-Bit ist ein harter Fehler statt stillem Fehl-Parse). **Robustheit (untrusted-Eingangspfad, Charta §8):** grenzen-geprüfter `Cursor`, truncierter/korrupter Record ⇒ `Cat034DecodeError` (Datagramm verworfen, kein Panic); Fuzz-Target `cat034_decode` (Seed-Korpus, CI-Job; lokal 13 M Läufe ohne Befund). I034/010 und I034/000 sind Pflicht-Items. | verifiziert | `firefly-asterix`: `cat034::north_marker_matches_reference_vector`, `cat034::sector_crossing_decodes_sector_and_rotation`, `cat034::multiple_records_in_one_block_decode`, `cat034::compound_items_are_skipped_length_correctly`, `cat034::compound_spare_bit_is_rejected`, `cat034::unknown_message_type_is_tolerated`, `cat034::missing_identity_or_type_is_rejected`, `cat034::wrong_category_and_length_mismatch_are_rejected`, `cat034::truncations_never_panic`, `cat034::overlong_fspec_chain_is_rejected_not_panicked`; Fuzzing: `fuzz/fuzz_targets/cat034_decode.rs` |
| FR-IO-010 | Ein **ASTERIX-CAT021-Datenblock** (ADS-B Target Reports, EUROCONTROL SUR.ET1.ST05.2000-STD-12, Edition 2.x) kann **dekodiert** werden — der Eingangs-Decoder für eine eigene ADS-B-Bodenstation (FEP.3, ARTAS-Roadmap AP-FEP). Neues Modul `firefly-asterix::cat021` (gemeinsame `fspec`-Mechanik): `decode_adsb_reports` liest je Record die track-relevanten Items — I021/010 (SAC/SIC, Pflicht), Position **I021/131** (hochauflösend, LSB 180/2³⁰ °) bevorzugt vor I021/130 (LSB 180/2²³ °), Zeit I021/073 → /071 → /077 (Fallback-Kette, 1/128 s), I021/080 (ICAO-Adresse), I021/170 (Callsign, 6-Bit-IA5), I021/070 (Mode 3/A), I021/140/145 (geometrische Höhe/Flugfläche), **I021/090 → NACp** (Qualitätsindikator), I021/040 (GBS/SIM/TST-Flags) — und **überspringt alle übrigen Items der 49-FRN-UAP längen-korrekt** über ein vollständiges Pro-FRN-Format-Modell (fixed/extended/repetitive/compound/explicit; Compound Met/Trajectory über Subfeld-Längenmodelle, Ages über FX-Kette). Ein Item auf einem Spare-FRN (43–47) ⇒ harter Fehler („edition mismatch" — eine 0.26-Station scheitert **laut** statt still falsch dekodiert). **Robustheit (untrusted-Eingangspfad, Charta §8):** grenzen-geprüfter `Cursor`, truncierter/korrupter Record ⇒ `Cat021DecodeError` (Datagramm verworfen, kein Panic); Fuzz-Target `cat021_decode` (Seed = Referenz-Vektor; Smoke 5,3 M Läufe ohne Befund). | verifiziert | `firefly-asterix`: `cat021::airborne_report_matches_reference_vector`, `cat021::position_prefers_high_resolution_and_signs_via_twos_complement`, `cat021::identity_quality_and_altitude_decode`, `cat021::target_identification_decodes_callsign`, `cat021::report_descriptor_flags_ground_and_test_targets`, `cat021::skips_unused_standard_items_length_correctly`, `cat021::malformed_input_is_rejected_not_panicked`, `cat021::spare_frn_is_rejected`, `cat021::overlong_fspec_chain_is_rejected_not_panicked`; Fuzzing: `fuzz/fuzz_targets/cat021_decode.rs` |
| FR-IO-011 | **Legacy-ASTERIX-Datenblöcke CAT001/CAT002** (Monoradar Target Reports / Service Messages, SUR.ET1.ST05.2000-STD-02a/02b) können **dekodiert** werden — die Vorgänger-Generation von CAT048/CAT034, die ein erheblicher Teil der Bestandsradare noch sendet (FEP.4, ARTAS-Roadmap AP-FEP). **CAT001** (`firefly-asterix::cat001`): Kern-Besonderheit ist die **zweigeteilte UAP** (Plot- vs. Track-Profil, Selektor = TYP-Bit in I001/020, FRN 2) — dasselbe FSPEC-Bit bedeutet je nach Record-Art ein anderes Item (FRN 3 = I001/040 Position im Plot- bzw. I001/161 Track-Nummer im Track-Profil); ein Record mit FRN ≥ 3 ohne I001/020 wird **abgelehnt statt geraten** (`MissingItem`). Gelesen: I001/010, /020 (TYP/SIM/SSR-PSR-2-Bit-Feld/SPI), /040 (RHO LSB **1/128 NM** — gröber als CAT048 —, THETA 360/2¹⁶ °), /070, /090, /161 und die **trunkierte Tageszeit I001/141** (u16 × 1/128 s, **wickelt alle 512 s**; als `truncated_time_secs` berichtet, nie als voller Zeitstempel erfunden); alle übrigen Items beider UAPs längen-korrekt übersprungen (Fixed/FX-Ketten/SP-Explicit); Spare-FRNs (Plot 16–19) und der nie genutzte **RFS-Indikator** (FRN 21) sind harte Fehler. `DecodedLegacyReport::into_target_report(time)` konvertiert in den CAT048-`DecodedTargetReport` (kein Mode S: ICAO/Callsign/DAPs leer). **CAT002** (`firefly-asterix::cat002`): liefert dasselbe `DecodedServiceMessage` wie CAT034 (Konsumenten format-agnostisch); Typ-Codes **explizit** gemappt (1 = Nordmarke, 2 = Sektor; Typ 3 ist hier der **Süd**-Marker → `Other`, nie mit CAT034s Geo-Filter-Code verwechselt); I002/030-ToD (voll, 1/128 s), Sektor, gemeldete Umlaufzeit; FX-Ketten/Repetitive/SP übersprungen, Spare-FRN 12 + RFS-FRN 14 harte Fehler. **Robustheit (Charta §8):** grenzen-geprüfte Cursor, kein Panic auf Eingabe; Fuzz-Targets `cat001_decode`/`cat002_decode` (Seeds = Referenz-Vektoren; Smoke 5,5 M/7,0 M Läufe ohne Befund). UAP-Tabellen gegen die generierte EUROCONTROL-Spezifikations-Referenz (asterix-specs/libasterix cat001 ed 1.4 / cat002 ed 1.2) verifiziert. | verifiziert | `firefly-asterix`: `cat001::plot_record_matches_reference_vector`, `cat001::track_record_reads_the_track_uap`, `cat001::typ_bit_selects_the_uap_per_record`, `cat001::descriptor_flags_and_extension_decode`, `cat001::item_without_uap_selector_is_rejected`, `cat001::spare_and_rfs_frns_are_rejected`, `cat001::skips_unused_items_length_correctly`, `cat001::conversion_into_target_report_carries_fields`, `cat001::wrong_category_and_length_mismatch_are_rejected`, `cat001::truncations_never_panic`, `cat001::overlong_fspec_chain_is_rejected_not_panicked`; `cat002::north_marker_matches_reference_vector`, `cat002::sector_crossing_decodes_sector_and_rotation`, `cat002::south_marker_and_blind_zone_codes_stay_other`, `cat002::skips_unused_items_length_correctly`, `cat002::spare_and_rfs_frns_are_rejected`, `cat002::missing_identity_or_type_is_rejected`, `cat002::wrong_category_and_length_mismatch_are_rejected`, `cat002::truncations_never_panic`, `cat002::overlong_fspec_chain_is_rejected_not_panicked`; Fuzzing: `fuzz/fuzz_targets/cat001_decode.rs`, `fuzz/fuzz_targets/cat002_decode.rs` |
| FR-NET-016 | Der `radar_asterix`-Eingang verarbeitet **Legacy-Radare (CAT001/CAT002)** auf demselben UDP-Socket wie CAT048/CAT034 (FEP.4) — **keine neue Quelle, kein Kontrakt-Bump**: ein Legacy-Radar wird unverändert als `radar_asterix` konfiguriert, der Listener verzweigt am führenden CAT-Oktett (`0x01`/`0x02` zusätzlich zu `0x22`/CAT048). (a) **Zeit-Anker** — CAT001-Records tragen nur die trunkierte Tageszeit (mod 512 s); der Listener führt den **letzten vollen ToD des Service-Stroms** (CAT002 I002/030, auch CAT034 I034/030) als Anker und expandiert per `expand_truncated_tod` (rein, testbar): der zum Anker **nächstgelegene** kongruente Wert (±256 s tolerant, Mitternachts-Wrap in den Vortags-Rest statt negativ). **Ohne Anker wird ein Legacy-Plot verworfen statt mit erfundener Zeit versehen** (ehrlich: verlorener Plot in der ersten Antennen-Umdrehung schlägt falsche Datenzeit). (b) **Plot-Pfad** — `legacy_datagram_to_plots` (rein): dekodiert, verwirft **simulierte** Meldungen (SIM-Bit; FR-TRK-036: Firefly führt keinen simulierten Verkehr), expandiert die Zeit und speist durch **dasselbe** `target_report_to_plot`-Mapping wie CAT048 — Tracker/Fusion/CAT063 sehen keinen Generations-Unterschied; CAT002-Nordmarken speisen den vorhandenen `ScanPeriodEstimator` (FR-NET-014) unverändert. Keine neuen Env-Variablen, keine neuen Metriken (Zähler `firefly_radar_plots_received_total` umfasst Legacy-Plots), Ausgabe-ICD unberührt. | verifiziert | `firefly-radar`: `legacy::expansion_picks_the_nearest_congruent_time`, `legacy::expansion_wraps_at_midnight`, `legacy::cat001_datagram_yields_anchored_plot`, `legacy::simulated_reports_are_dropped`, `legacy::cat002_datagram_yields_service_messages` (Listener-Dispatch baut auf `listener::cat034_datagram_yields_service_messages`/FR-NET-014 auf) |
| FR-IO-012 | **ASTERIX-Datenblöcke CAT020/CAT019** (Multilateration Target Reports / System Status, SUR.ET1.ST05.2000-STD-14/18) können **dekodiert** werden — der WAM/MLAT-Eingang (FEP.5, ARTAS-Roadmap AP-FEP). **CAT020** (`firefly-asterix::cat020`): gelesen werden I020/010 (Pflicht), I020/140 (**volle** ToD), Position **I020/041 WGS84 hochauflösend** (i32, LSB 180/2²⁵ ° — wie I062/105; I020/042 kartesisch übersprungen), I020/070 Mode 3/A, I020/090 Flugfläche, I020/105 geometrische Höhe (LSB 6,25 ft), I020/220 ICAO-Adresse, I020/245 Callsign (STI-Oktett + 8×6-Bit-IA5), I020/161 Track-Nummer, Descriptor-Flags aus I020/020 ext1 (**RAB** Feldmonitor, **SIM/TST**, **GBS**, SPI) und **I020/500 SDP** — die per-Meldungs-Standardabweichung der Positionslösung (σx/σy u16 × 0,25 m; berichtet als konservatives `max(σx,σy)`; SDP absent → kein σ-Anspruch). Alle übrigen Items der 28-FRN-UAP längen-korrekt übersprungen (Fixed/FX-Ketten/Repetitive I020/400 (1 Okt.)+I020/250 (8 Okt.)/RE/SP); ein gesetztes Spare-/FX-Bit im I020/500-Spec-Oktett und unbekannte FRNs sind harte Fehler. **CAT019** (`firefly-asterix::cat019`): Message Type (1 = Start of Update Cycle, 2 = Periodic, 3 = Event, sonst `Other`), ToD, **I019/550-NOGO** (2 Bit: nur 0 = operational-Anspruch; absent = kein Anspruch) — als `DecodedMlatStatus` für die Liveness-Speisung; übrige Items (551/552/553/600/610/620/RE/SP) übersprungen, Spare-FRNs 11/12 harte Fehler. **Robustheit (Charta §8):** grenzen-geprüfte Cursor, kein Panic auf Eingabe; Fuzz-Targets `cat020_decode`/`cat019_decode` (Seeds = Referenz-Vektoren; Smoke 4,2 M/5,0 M Läufe ohne Befund). UAP-Tabellen gegen die generierte EUROCONTROL-Referenz (asterix-specs/libasterix cat020 ed 1.11 / cat019 ed 1.3) verifiziert. | verifiziert | `firefly-asterix`: `cat020::airborne_report_matches_reference_vector`, `cat020::position_accuracy_yields_sigma`, `cat020::identity_altitude_and_flags_decode`, `cat020::skips_unused_items_length_correctly`, `cat020::unknown_frn_is_rejected`, `cat020::malformed_input_is_rejected_not_panicked`; `cat019::periodic_status_matches_reference_vector`, `cat019::nogo_and_message_types_decode`, `cat019::skips_unused_items_length_correctly`, `cat019::spare_frns_and_missing_items_are_rejected`, `cat019::malformed_input_is_rejected_not_panicked`; Fuzzing: `fuzz/fuzz_targets/cat020_decode.rs`, `fuzz/fuzz_targets/cat019_decode.rs` |
| FR-NET-017 | Firefly empfängt **WAM/MLAT** über **ASTERIX CAT020/CAT019 via UDP** und speist die Zielmeldungen als **geodätische Plots** in denselben Tracking-Kanal wie Radar/ADS-B (FEP.5, ARTAS-Roadmap AP-FEP — damit sind alle vier klassischen ARTAS-Eingangsklassen bedient: Radar 048/034 + 001/002, ADS-B 021, WAM 020/019). Eigene Crate `firefly-mlat` (Ports & Adapters, spiegelt `firefly-adsb021`): `MlatConfig` (12-Factor, `FIREFLY_MLAT_*`; Sensor-Default 240, Port 8020; **kein Standort** — CAT020-Positionen sind geodätisch, vom Bodensystem berechnet); `mlat_report_to_plot` bildet einen `DecodedMlatReport` (FR-IO-012) auf einen `Plot` ab: **σ je Meldung aus I020/500 SDP** (fehlend → konservative 150 m — ein System ohne Qualitätsangabe wird nicht über-vertraut), Höhe geometrisch → barometrisch → 0, **Drop-Regeln**: Feldmonitor (RAB), Simulations-/Test- (SIM/TST), Boden- (GBS) sowie positions-/zeitlose Meldungen gelangen nie ins Luftlagebild; `SourceKind::ModeS` (ehrlich zur zugrundeliegenden Technologie; eigenes MLT-Provenienz-Subfeld = ICD-Bump, bewusst Folge-Häppchen). Listener dispatcht am CAT-Oktett: CAT020 → Plots, **CAT019 → Status-Callback** (jede Statusmeldung = Sensor-Aktivität → CAT063-Liveness ohne Verkehr; selbst-gemeldetes degraded/NOGO wird als WARN geloggt). **Verdrahtung:** `mlat_config_from_spec` (Kontrakt v1.7.0: `listen`?/`sac`?/`sic`?/`sensor_id`? — keine bbox, kein `cred_env`), Fallback `FIREFLY_MLAT_ENABLED`, `spawn_mlat_listener_live` in den geteilten `mpsc`, Registrierung als geodätischer Sensor (Nominal 5 s), Metriken `firefly_mlat_reports_received_total`/`firefly_sources_mlat` (NFR-OBS-001). Ohne bbox kein Beitrag zum System-Referenzpunkt → als Einzelquelle `FIREFLY_SYSTEM_REF_*` setzen (dokumentiert). Ausgabe-ICD unverändert. | verifiziert | `firefly-mlat`: `config::empty_environment_yields_defaults`, `config::values_parse_and_garbage_falls_back`, `plot::airborne_report_maps_to_geodetic_plot`, `plot::missing_sdp_gets_the_conservative_default`, `plot::drop_rules_keep_the_air_picture_clean`, `plot::altitude_falls_back_from_geometric_to_baro_to_zero`, `listener::valid_datagram_yields_a_plot_and_garbage_none`, `listener::cat019_datagram_yields_status`; `firefly-server`: `sources::mlat_config_maps_identity_and_listen`, `sources::mlat_minimal_spec_uses_defaults_and_bad_fields_are_errors`, `sources::resolve_routes_mlat_sources`, `metrics::render_includes_all_metrics` |
| FR-TRK-046 | Firefly führt je Radar-Sensor eine **räumliche Clutter-Karte** und eine **Multipath-Reflexions-Heuristik** (SPEC.2, ARTAS-Roadmap AP-SPEC; ADR 0037): (a) **Karte** (`firefly-track::clutter_map`) — polares Raster (5-km-Ringe × 64 Sektoren), je Zelle exponentiell vergessene Ereignisrate (τ = 600 s), gelernt aus **unassoziierten** Plots (Initiierungs-Kandidaten; Gründungs-Plots echter Ziele waschen aus); datenzeit-deterministisch, O(1)/Plot, snapshot-fähig (Tupel-Schlüssel als Paar-Liste). **Floor = Default:** ohne Expositions-Buchführung ist Ereignis-Abwesenheit kein Sauberkeits-Beleg — die Karte hebt λ nur an (Deckel 100×), nie darunter (Erst-Entwurf mit 0,1×-Floor benachteiligte messbar reale Ziele und wurde zurückgenommen; Regressions-Test hält). (b) **Per-Track-λ in JPDA** (`joint_association_probabilities_local`): jeder Track assoziiert unter der Dichte der Zelle, durch die er fliegt (Clutter-Term = Faktor unassignierter Tracks in den Joint-Events); bestehende Signatur delegiert mit uniformen Dichten. (c) **Reflexions-Heuristik:** Primary-only-Neugeburt ±2° hinter (≥ 500 m) einem bestätigten Track desselben Radars ⇒ `reflection_suspect`; Wirkung ausschließlich **Bestätigungs-Schwelle +2 Hits** (verzögert, nie exekutiert — ein echtes Ziel hinter einem anderen bestätigt später); SSR-/Mode-S-Identität löscht den Verdacht. **(d) Expositions-Buchführung (SPEC.2b, ADR-0037-Nachtrag):** Die Karte kreditiert Beobachtungszeit je Sensor-Batch (`mark_active`, Lücken-Kredit max. 30 s — Ausfall reift nie); ab 1200 s Reife (2τ, je Zelle ab erstem Ereignis, ereignisfreie Zellen ab Karten-Start) sinkt der Floor auf 0,1 × Default — nachweislich ruhige Regionen dürfen ehrlich unter den Default; unreife Evidenz behält ihn (schützt die Gründungs-Plot-Regression). **(e) Metrik** `firefly_clutter_cells` (Gauge) über die On-Tick-Kette. Kein Wire-/ICD-Bezug, keine Env-Variablen. | verifiziert (inkl. SPEC.2b) | `firefly-track`: `clutter_map::unlearned_cells_report_the_default`, `clutter_map::hotspot_raises_only_its_own_cell`, `clutter_map::stale_hotspots_decay`, `clutter_map::density_is_clamped_to_the_band`, `clutter_map::azimuth_wraps_cleanly`, `clutter_map::snapshot_roundtrip_preserves_cells`, `jpda::coalescence_tests::local_density_raises_beta0_in_hotspots`, `tracker::clutter_map_learns_from_unassociated_plots`, `tracker::reflection_suspect_confirms_late_but_is_not_executed`, `tracker::ssr_target_behind_another_is_no_suspect`; SPEC.2b: `clutter_map::mature_exposure_lets_quiet_cells_drop_below_the_default`, `clutter_map::immature_evidence_keeps_the_default_floor`, `clutter_map::outages_do_not_accrue_exposure`, `metrics::render_includes_all_metrics`; Regression `tracker::jpda_keeps_two_close_parallel_tracks_distinct` + `snapshot::*` unverändert grün |
| FR-TRK-045 | Firefly behandelt **Duplikat-Identitäten und JPDA-Koaleszenz** als erkannte Sonderfälle (SPEC.1, ARTAS-Roadmap AP-SPEC; ADR 0036): (a) **Identität als weicher Schlüssel** — der ICAO-Fastpath (FR-TRK-031) assoziiert nur noch **innerhalb des kinematischen Gates** hart; ein ICAO-Match weit außerhalb ist Evidenz für zwei Luftfahrzeuge mit derselben Adresse (Transponder-Fehlkonfiguration; ORCAM-Squawk-Wiederverwendung an Grenzen — Lektion Weeze; Conspicuity 1000 per Design mehrdeutig) und fällt in JPDA/Initiierung durch, wo es einen **eigenen Track** gründet, statt den Träger-Track teleportieren zu lassen. (b) **Duplikat-Scan** — nach jeder Fusions-Gelegenheit werden alle lebenden Tracks nach gleicher ICAO **oder** gleichem Mode 3/A gruppiert; jede Gruppe > 1 setzt auf allen Trägern das `identity_conflict`-Flag (WARN-Log beim Neuauftreten; automatisch gelöscht, wenn ein Träger verschwindet; deterministischer Voll-Rescan). Niemals Merge, keine harte Identitäts-Assoziation solange der Konflikt steht. (c) **Koaleszenz-Wächter** (`jpda::decouple_coalescing_pairs`) — Track-Paare, deren Positionen unter kombinierter Kovarianz statistisch unauflösbar sind (d² < 4, d. h. 2σ), bekommen ihre **geteilten** Mess-Hypothesen beschnitten: jede geteilte Messung behält nur der stärkere Anwärter, die abgegebene β-Masse wandert in dessen β₀ (Zeilensumme bleibt 1; Gleichstand → niedrigerer Index, deterministisch). Gut getrennte Paare und ungeteilte Messungen unberührt — im Normalverkehr ein No-op. Messbar: ohne Wächter driftet ein 150-m-Parallel-Paar in oszillierenden Zügen auf ≤ 113 m zusammen, mit Wächter hält es 148–150 m (Negativ-Test verifiziert, dass der Test beißt). (d) **Registrierungs-Schutz** — eine Identitäts-Korrespondenz, deren gehobene Positionen > 5 km auseinanderliegen, ist Identitäts-Kontamination statt Bias-Evidenz und wird vor der Least-Squares-Schätzung verworfen (echte Registrierungsfehler sind Größenordnungen kleiner). Kein Wire-/ICD-Bezug; keine neuen Env-Variablen. | verifiziert | `firefly-track`: `tracker::icao_match_is_gated_and_duplicates_stay_separate`, `tracker::icao_match_inside_the_gate_still_associates_directly`, `tracker::unresolvable_parallel_targets_do_not_coalesce` (End-to-End, Negativ-Check dokumentiert), `jpda::coalescence_tests::unresolvable_pair_gets_exclusive_assignments`, `jpda::coalescence_tests::resolved_pair_is_left_alone`; Registrierungs-Bestand (`registration::*`) unverändert grün unter dem Separations-Deckel |
| FR-TRK-044 | Firefly besitzt das **6-D-Zustandsfundament für das Constant-Acceleration-IMM-Modell** (VERT.4a, ARTAS-Roadmap AP-VERT; ADR 0035, Weg A — einheitlich 6-D innerhalb der Bank, 4-D-Projektion am Rand; **bewusst noch nicht in die Bank verdrahtet**, Integration = VERT.4b): (a) **`firefly-track::kalman6::LinearKalman6`** — der 6-D-Zwilling des 4-D-Filters auf `[E, N, vE, vN, aE, aN]`, Numerik-Spiegel von `LinearKalman` (Position-only-`H`, geteilte Innovation, Gauß-Likelihood mit `2π·√|S|`-Normierung, **Joseph-Form**-Update); serialisierbar, datenzeit-getrieben (ADR 0003). (b) **6-D-Transitionen mit ehrlicher Beschleunigungs-Aussage je Hypothese**: CA `p' = p + v·dt + a·dt²/2` (Halbgruppen-Eigenschaft `F(a+b) = F(b)·F(a)`); CV mit **Null-Zeilen** für die Beschleunigung („es gibt keine Beschleunigung" — eingemischte Beschleunigung wird genullt, F bewusst singulär); CT mit **Zentripetal-Zeilen** `a' = ω·J·v'` (linear im Zustand — eine stationäre Kurve meldet ihre wahre Querbeschleunigung statt einer falschen Null; ohne diesen Kniff wäre I062/210 in Kurven schlechter als der VERT.3-Ableiter); (p,v)-Blöcke exakt gleich den 4-D-Transitionen, Kleinst-Raten-Guard wie `motion.rs`. (c) **`JerkNoise`** — White-Noise-Jerk-Prozessrauschen (CWNA eine Ableitung höher), `Q`-Triple pro Achse `q·[dt⁵/20, dt⁴/8, dt³/6; …]`, symmetrisch/positiv definit/achsen-entkoppelt/linear in der PSD. (d) **Weg-A-Rand-Abbildungen**: `from_kalman4` (Einbettung: a = 0, diagonaler Prior, keine behauptete Kreuz-Kovarianz) und `to_kalman4` (exakte Gauß-Marginale) — Einbettung → Projektion ist Identität auf (p, v); der nachgelagerte Fusionskern (Gating/JPDA/Registrierung) bleibt auf seinem unveränderten 4-D-Vertrag. Kein Wire-/ICD-Bezug, keine Verhaltens-Änderung (rein additives Fundament). **Integration (VERT.4b):** Die IMM-Bank läuft vollständig auf `LinearKalman6` (Mischung/Kombination/PDA-Update in 6-D, `Imm::new` bettet 4-D-Seeds ein, `predict` via `MotionModel::transition6` + modellspezifischem Q: CV/CT = CWNA-(p,v)-Block + Konditionierungs-Floor, CA = Jerk); `combined_estimate()` liefert unverändert die exakte 4-D-Marginale (Kern-Vertrag eingehalten), neu `combined_estimate6`/`combined_acceleration`. **Default-Bank = `cv_turns_and_ca`** (CV klebrig 0,94, CA-Einstieg 0,02–0,03 — ein viertes Modell besteuert sonst messbar die Geradeaus-RMSE; nachgetuned gegen den Szenario-Test). **I062/210 kommt aus dem Filterzustand** (`Track::acceleration_mps2` → `imm.combined_acceleration()`; der VERT.3-Ableiter bleibt als deterministischer Frische-Zeuge und Erst-Schätzungs-Gate, sein Glättungswert ist abgelöst — auch der LONG-Trend projiziert jetzt den Filterzustand; Zentripetal-Anteil steht ⊥ v und kontaminiert LONG nicht). **Ehrliche Grenze:** die kombinierte Beschleunigung ist die MMSE-Mischung — bei mehrdeutiger Evidenz (sanfte 1 m/s² bei 4-s-Scans/σ=30 m ⇒ ~0,27σ CV-Lag/Scan) um den CV/CT-µ-Anteil geschrumpft; ein realer Startlauf (~2,5 m/s²) trennt scharf (µ_CA > 0,7). Snapshot-Layout gebrochen (6-D) — vor HA.1 ohne produktives Restore-Format. | verifiziert (VERT.4a Fundament + VERT.4b Integration) | `firefly-track`: `kalman6::ca_transition_predicts_uniform_acceleration_exactly`, `kalman6::cv6_and_ct6_embed_their_4d_counterparts`, `kalman6::ct6_reports_centripetal_acceleration`, `kalman6::jerk_q_is_symmetric_positive_definite_and_scales`, `kalman6::filter_estimates_acceleration_as_state` (Kernnachweis: 0,5 m/s² aus reinen Positionsmessungen als **Filterzustand** geschätzt, keine Phantom-Querbeschleunigung), `kalman6::embedding_and_projection_are_consistent`, `kalman6::update_shrinks_uncertainty_and_keeps_p_symmetric`; `imm::ca_model_wins_on_an_accelerating_target_and_reports_the_state`, `imm::steady_turn_reports_centripetal_acceleration`; End-to-End-Regression `metrics::single_target_quality_meets_thresholds` (RMSE-Schwelle mit 4-Modell-Bank gehalten) |
| FR-TRK-043 | Firefly bestimmt je Track den **Mode of Movement** (Kurs-/Geschwindigkeits-/Vertikal-Trend) und die **horizontale Beschleunigung** und bringt beide auf den Draht (VERT.3, ARTAS-Roadmap AP-VERT; ICD 3.6.0, additiv): (a) **Beschleunigungs-Schätzer** (`firefly-track::acceleration::AccelerationEstimator`): differenziert konsekutive **IMM-Kombinationsschätzungen** der Geschwindigkeit über die Datenzeit und glättet per EWMA (α = 0,3); Samples mit Abstand < 0,5 s werden **übersprungen** (Differenzenquotient über nahe-Null-Basis verstärkt Jitter statt Information — Multi-Sensor-Treffer binnen Millisekunden); datenzeit-getrieben (ADR 0003), serialisierbar. **Bewusste Architektur-Entscheidung (Scope-Split):** kein 6-D-CA-Modell in der IMM-Bank — das würde den gesamten 4-D-Fusionskern (LinearKalman/Gating/JPDA/Registrierung) durchschneiden; die Bank-Erweiterung ist als eigenes Häppchen **VERT.4** (Roadmap, S5, eigener ADR) ausgewiesen. VERT.3 = Anzeige-Hälfte (abgeleitete Beschleunigung auf dem Draht); VERT.4 = Tracking-Hälfte (Filterzustand + bessere Prädiktion). (b) **Trend-Ableitung** (`Track::mode_of_movement`): **Kurs-Trend** aus den vorhandenen **CT-Modellwahrscheinlichkeiten** der IMM-Bank (Σµ der Rechts-/Links-Dreh-Modelle; Drehung erst bei µ > 0,5, positive mathematische Rate = Linkskurve; Bank ohne Dreh-Modelle → `Undetermined`); **Speed-Trend** aus der Along-Track-Komponente der Beschleunigung (Schwelle 0,2 m/s², erst ab 5 m/s Grundgeschwindigkeit — darunter ist „along-track" nicht wohldefiniert); **Vertikal-Trend** aus der Vertikal-Filter-Rate (FR-TRK-042; Schwelle ±300 ft/min). Jede Kette ohne frische Basis (30 s, PROVENANCE_FRESH_S) meldet `Undetermined` — Absenz statt Behauptung. (c) **Draht** (ICD 3.6.0, additiv): **I062/200** (FRN 15, 1 Oktett: TRANS Bits 8–7, LONG 6–5, VERT 4–3, ADF Bit 2 immer 0 — Firefly meldet keine Altitude-Discrepancy) **nur wenn mindestens ein Trend bestimmt ist** (`ModeOfMovement::is_determined`); **I062/210** (FRN 8, Ax/Ay i8 × 0,25 m/s², Sättigung statt Wrap) nur bei frischem Schätzwert; Track ohne beides byte-identisch zur Vor-3.6.0-Form (kein Wire-Bruch); byte-genaue Referenz-Vektoren, Decoder liest beide zurück. Wayfinder-Nachzug additiv ohne Lockstep (Issue folgt). | verifiziert | `firefly-track`: `acceleration::constant_velocity_zero_and_speedup_converges`, `acceleration::near_simultaneous_samples_are_skipped`, `track::mode_of_movement_derives_from_the_chains`; `firefly-asterix`: `cat062::kinematics_items_encode_byte_exactly_and_round_trip`; Fuzzing: bestehendes `cat062_decode`-Target deckt die neuen Decode-Pfade (Smoke 7,0 M Läufe ohne Befund) |
| FR-TRK-042 | Firefly führt ein **Vertikal-Tracking je Track** und bringt die Vertikal-Kette auf den Draht (VERT.2, ARTAS-Roadmap AP-VERT; ICD 3.5.0, additiv): (a) **Vertikal-Filter** (`firefly-track::vertical::VerticalFilter`, 2-Zustands-Kalman im **Druckhöhen-Raum**: Höhe + Rate, CWNA-Manöverbudget `ACCEL_PSD = 4 (ft/s²)²/Hz`, Mess-σ 12 ft aus der 25-ft-Quantisierung): jede Mode-C-/FL-Meldung eines assoziierten Plots speist ihn via `update_identity`; **Gating** (5σ) verwirft Mode-C-Ausreißer (Garbling), **3 konsekutive Rejects reinitialisieren** auf das neue Level (ein persistenter „Ausreißer" ist ein echter Level-Sprung, den das Gate sonst ewig aushungern würde); datenzeit-getrieben (ADR 0003), serialisierbar (Snapshot/Restore). (b) **Geometrische Höhe getrennt**: `ModeAC.geometric_height_ft` (neu, serde-default) wird **nur** von Adaptern mit echt geometrischer Quelle gesetzt (ADS-B-Station I021/140, MLAT I020/105); der Track glättet sie als eigene EWMA (α = 0,3) — barometrisch und geometrisch werden **nie gemischt** (verschiedene Referenzen). (c) **Frische-Disziplin**: `vertical_estimate`/`geometric_altitude_ft` liefern nur innerhalb 30 s (PROVENANCE_FRESH_S) — Absenz statt Stale-Behauptung; `SystemTrack` trägt `barometric_altitude_ft`/`barometric_qnh_corrected`/`geometric_altitude_ft`/`rocd_ft_min` (serde-default). (d) **QNH am Ausgang** (`firefly-server::live::apply_qnh`): vor der Publikation wird je Track das QNH an der Track-Position nachgeschlagen (FR-TRK-041); **nur ein beobachtetes regionales QNH** korrigiert (exakte ICAO-Barometrie) und setzt das Flag — Standardatmosphäre ⇒ Druckhöhe unverändert, Flag 0, nie eine stille Schein-Korrektur. (e) **Draht** (ICD 3.5.0, additiv): **I062/130** (FRN 18, i16 × 6,25 ft), **I062/135** (FRN 19, QNH-Bit + 15-Bit-ZK × 25 ft), **I062/220** (FRN 20, i16 × 6,25 ft/min); Absenz bei fehlendem/stalem Schätzwert, Track ohne Vertikal-Daten byte-identisch zur Vor-3.5.0-Form, I062/136 unverändert daneben; byte-genaue Referenz-Vektoren; Decoder liest alle drei zurück (inkl. QNH-Bit, negative Werte). Wayfinder-Nachzug additiv ohne Lockstep (Issue folgt). | verifiziert | `firefly-track`: `vertical::level_flight_converges_and_smooths_quantisation`, `vertical::steady_climb_is_followed`, `vertical::garbled_reply_is_gated_out`, `vertical::persistent_new_level_reinitialises`, `track::vertical_chain_reports_fresh_and_withholds_stale`; `firefly-asterix`: `cat062::vertical_items_encode_byte_exactly_and_absence_is_unchanged`, `cat062::decode_recovers_vertical_items`; `firefly-server`: `live::apply_qnh_corrects_only_observed_regions`; Fuzzing: bestehendes `cat062_decode`-Target deckt die neuen Decode-Pfade (Smoke 5,5 M Läufe ohne Befund) |
| FR-TRK-041 | Firefly führt einen **Meteo/QNH-Dienst** (VERT.1, SDPS-003-Analogon) als Fundament der Vertikal-Kette: Mode-C/Flugflächen sind Druckhöhen (Referenz 1013,25 hPa); unterhalb der Transition Altitude braucht die wahre Höhe das lokale QNH (~27–30 ft Fehler je hPa; ein kräftiges Tief ⇒ > 800 ft). Neue Crate `firefly-meteo`: (a) **`QnhService`** — Menge von QNH-Regionen (`{name, lat, lon, radius_nm?, qnh_hpa}`), `lookup(lat, lon)` wählt die **nächstgelegene anwendbare** Region (Radius fehlt = unbegrenzt, konkurriert über Nähe; Haversine); **ohne anwendbare Region → Standardatmosphäre, explizit als `QnhSource::StandardAtmosphere` gekennzeichnet** — „keine Daten" und „gemessene 1013 hPa" sind operativ verschiedene Aussagen, ein QNH wird nie erfunden. (b) **`pressure_altitude_to_qnh_altitude`** — exakte ICAO-Troposphären-Umrechnung (Druckhöhe → statischer Druck → Höhe in QNH-Atmosphäre; κ = R·L/g₀ = 0,1902632), nicht die lineare 27-ft/hPa-Faustregel: bei Standard-QNH Identität, für kleine Abweichungen fällt die Faustregel heraus (per Test verifiziert), Monotonie in QNH. (c) **`MeteoConfig`** — 12-Factor: `FIREFLY_METEO_QNH` (JSON-Array); malformes JSON, leerer Name, Koordinaten außerhalb WGS84, Radius ≤ 0 oder QNH außerhalb des Plausibilitätsbands **[870, 1085] hPa** (Rekord-Extreme) → **Startfehler** (eine konfigurierte-aber-kaputte Meteo-Quelle degradiert nie still); unset/leer = leere Konfiguration (erlaubt, INFO im Log). **Verdrahtung:** Server parst beim Start (Fehler fatal), exponiert `firefly_meteo_qnh_regions` + `firefly_meteo_qnh_hpa{region=…}` (NFR-OBS-001) und loggt den Modus. **Ehrliche Grenzen:** env-getriebener Provider (Betreiber/Orchestrator aktualisiert extern im Wetter-Zyklus); Live-METAR-Provider = Folge-Häppchen mit eigenem ADR; die **Verwertung** im Höhen-Tracking (I062/135) ist VERT.2 — kein Wire-/ICD-Bezug in VERT.1. | verifiziert | `firefly-meteo`: `qnh::nearest_applicable_region_wins`, `qnh::outside_all_radii_falls_back_to_standard_atmosphere`, `qnh::unbounded_region_applies_everywhere_but_proximity_decides`, `altitude::standard_qnh_is_the_identity`, `altitude::correction_matches_the_rule_of_thumb_near_sea_level`, `altitude::conversion_is_monotonic_in_qnh`, `config::valid_json_parses_and_unset_is_empty`, `config::malformed_or_implausible_values_are_errors`; `firefly-server`: `metrics::render_includes_all_metrics`, `metrics::registration_bias_gauges_absent_without_estimates` (Absenz der QNH-Gauge) |
| FR-TRK-040 | **Mode-S-DAPs end-to-end** (FEP.2, ARTAS-Roadmap AP-FEP; ICD 3.4.0, additiv): Die Downlink Aircraft Parameters eines Mode-S-EHS-Radars fließen vom CAT048-Eingang bis auf den CAT062-Draht. (a) **BDS-Decoder** (`firefly-asterix::bds`, bit-genau nach ICAO Doc 9871): BDS 4,0 (MCP/FCU Selected Altitude, LSB 16 ft), BDS 5,0 (Roll LSB 45/256°, True Track LSB 90/512° signed→[0,360), Ground Speed/TAS LSB 2 kt), BDS 6,0 (Magnetic Heading LSB 90/512° signed→[0,360), IAS LSB 1 kt, Mach LSB 0,004, Baro-Vertikalrate LSB 32 ft/min signed). **Status-Bit-Disziplin:** jedes Feld nur bei gesetztem Status-Bit — kein Feld wird aus Nullen geraten (`None` ≠ 0); unbekannte Register ⇒ leeres `Daps`. Reine Bit-Arithmetik über festem 7-Byte-Array, kein Panic. (b) **CAT048:** I048/250 (bisher Skip) dekodiert je Repetition 7 Byte MB + BDS-Register-Oktett; mehrere Register **mergen per Feld** in `DecodedTargetReport.daps`. (c) **Datenmodell:** `firefly_core::Daps` (9 Option-Felder, serde-default für `.ffplots`-Kompatibilität) auf `ModeAC`; der `Track` merged per Feld (verschiedene Register kommen in verschiedenen Meldungen) und führt `daps_time`; `SystemTrack.daps` trägt die Werte **nur solange frisch** (`fresh_daps`, Fenster = `PROVENANCE_FRESH_S` = 30 s — eine veraltete Selected Altitude als aktuell zu zeigen wäre irreführend; Absenz statt Stale-Behauptung). (d) **I062/380-Ausbau** (compound, FX-verkettete Subfeld-Spec, Daten in aufsteigender Subfeld-Nummer): **MHG** (#3, LSB 360/2¹⁶ °), **SAL** (#6, SAS=1/Source=10=MCP + 13-Bit-Zweierkomplement, LSB 25 ft, Sättigung), **IAR** (#26, LSB 1 kt), **MAC** (#27, LSB 0,008); DAP-loser Track **byte-identisch** zur Vor-3.4.0-Form (`0x80`+Adresse), erst IAR/MAC verlängern die Spec via FX auf 4 Oktette. Decoder liest subfeld-getrieben zurück (`DecodedRecord.magnetic_heading_deg`/`selected_altitude_ft`/`ias_kt`/`mach`); unbekanntes Subfeld ⇒ Fehler statt Fehl-Parse; Spec-Kette begrenzt (max. 4 Oktette). **Fachlicher Kern:** SAL = im Autopiloten eingedrehte Höhe (Level-Bust-Erkennung im ASD). Wayfinder-Nachzug additiv ohne Lockstep (Issue #238). | verifiziert | `firefly-asterix`: `bds::bds_4_0_decodes_selected_altitude`, `bds::bds_5_0_decodes_track_and_turn`, `bds::bds_6_0_decodes_heading_and_speed`, `bds::cleared_status_bit_yields_none`, `bds::unknown_register_yields_empty_daps`, `cat048::mode_s_mb_data_decodes_and_merges_ehs_daps`, `cat062::aircraft_derived_data_encodes_dap_subfields_byte_exactly`, `cat062::dap_subfields_round_trip` (inkl. negativer SAL), `cat062::target_address_uses_adr_subfield_with_24_bit_address` (Alt-Form byte-identisch); `firefly-track`: `track::daps_merge_across_reports_and_report_only_while_fresh`; Fuzzing: bestehende `cat048_decode`/`cat062_decode`-Targets decken die neuen Pfade (Smoke 9,7 M Läufe ohne Befund) |
| FR-NET-014 | Der Radar-Eingang nutzt CAT034-Servicemeldungen zur **Sensor-Überwachung aus dem Datenstrom** (FEP.1, ARTAS-FEP-Funktion): (a) **Dispatch** — der `radar_asterix`-UDP-Listener verzweigt am führenden CAT-Oktett (`0x22` = 34 → Service-Pfad, sonst CAT048 → Plot-Pfad wie bisher); malformte Datagramme werden pfad-gleich verworfen (`datagram_to_service`). (b) **Gemessene Scan-Periode** — `firefly-radar::ScanPeriodEstimator` (rein, **datenzeit-getrieben** ADR 0003, ein Estimator je Radar-Quelle) misst die echte Antennen-Umlaufzeit aus den **Nordmarken-Intervallen**: Plausibilitätsband 1–60 s (Duplikate/Feed-Lücken verworfen), Mitternachts-Wrap von I034/030 korrigiert, **verpasste Nordmarke** (verlorenes Datagramm ⇒ verdoppeltes Intervall, > 50 % Abweichung) verworfen statt eingemittelt, akzeptierte Intervalle exponentiell geglättet (α = 0,25). (c) **Wirkung** — die gemessene Periode ersetzt den konfigurierten Nominalwert als **CAT063-Staleness-Basis** (`SensorHealthMonitor::update_scan_period`, Timeout = 2,5 × gemessene Periode, in beide Richtungen; nicht-finite/nicht-positive Werte ignoriert) und erscheint als Metrik `firefly_radar_scan_period_seconds{sensor=…}` + Zähler `firefly_radar_north_markers_total`; **jede** Servicemeldung zählt als Sensor-Aktivität (Liveness ohne Verkehr — „leerer Himmel" ≠ „totes Radar" eingangsseitig). **Ehrliche Grenze (FEP.1):** Die Tracker-Löschkadenz bleibt beim Konfigurationswert (dynamische Nachführung = eigenes Folge-Häppchen); ohne Servicemeldungen vom Radarkopf gilt unverändert das konfigurierte Verhalten. Kein Wire-/ICD-Bezug (reiner Eingangs-Pfad). | verifiziert | `firefly-radar`: `service::steady_rotation_is_measured_exactly`, `service::drifting_rotation_is_followed_smoothly`, `service::missed_marker_is_rejected_not_averaged`, `service::midnight_wrap_measures_the_true_revolution`, `service::implausible_deltas_are_rejected`, `listener::cat034_datagram_yields_service_messages`; `firefly-multicast`: `sensor_health::measured_scan_period_overrides_the_configured_timeout`, `sensor_health::implausible_measured_periods_are_ignored`; `firefly-server`: `metrics::render_includes_all_metrics`, `metrics::registration_bias_gauges_absent_without_estimates` (Absenz der Perioden-Gauge) |
| FR-NET-010 | Der CAT063-Sensor-Status wird als **UDP-Multicast** auf **dieselbe** Gruppe/Port wie CAT062/CAT065 gesendet (ADR 0022), als eigener, entkoppelter Sende-Task im **wall-clock-Takt** (Default 5 s) — ein Echtzeit-Lebenssignal, das den datenzeit-getriebenen Track-Pfad und den Tracker-Kern nicht berührt. Die Per-Sensor-Liveness liefert ein `SensorHealthMonitor` (`firefly-multicast::sensor_health`): er verfolgt je Sensor die Wall-clock-Zeit des letzten Plot-Batches und meldet einen Sensor als **aktiv**, solange dessen letzter Plot innerhalb von `2.5 × scan_period` liegt, sonst **degradiert**. Zwei Modi: `new_replay` (alle Sensoren dauerhaft aktiv — deterministische Wiedergabe meldet keine Degradierung) und `new_live` (Liveness aus dem echten OpenSky-Plot-Eingang via `record_activity`). `firefly-multicast::run_cat063_sender`; läuft, sobald Feed (`FIREFLY_CAT062_ENABLED`) **und** Heartbeat (`FIREFLY_CAT065_ENABLED`, Default an) aktiv sind; `FIREFLY_CAT063_PERIOD` (Default 5 s). `firefly-server` startet den Task (`spawn_cat063_sensor_sender`), stempelt die UTC-Tageszeit ein und exponiert `firefly_cat063_status_sent_total` (Counter), `firefly_sensors_total` und `firefly_sensors_active` (Gauges, NFR-OBS-001). SAC der Sensor-Records = 0 (Firefly-Konvention); die SICs sind die der registrierten Sensoren (`scene_sensor_ids`). | verifiziert | `firefly-multicast`: `cat063_sender::cat063_blocks_are_sent_and_decodable`, `sensor_health::recently_seen_sensor_is_active`, `sensor_health::stale_sensor_becomes_inactive`, `sensor_health::sensor_within_threshold_is_active`, `sensor_health::multi_sensor_counts_are_correct`, `sensor_health::sensor_isolation_is_maintained`, `sensor_health::replay_mode_all_sensors_start_active`, `sensor_health::never_seen_sensor_is_inactive_in_live_mode`, `sensor_health::unregistered_sensor_id_is_ignored`, `sensor_health::empty_monitor_gives_zero_counts`, `sensor_health::sensors_total_is_stable`; `config::heartbeat_config_is_parsed_with_safe_defaults` (CAT063-Periode-Default); `firefly-server`: `metrics::render_includes_all_metrics` |
| FR-NET-011 | Firefly liest die Live-Quellen einer **orchestrierten Instanz** aus dem env-getriebenen **Quell-Eingangs-Kontrakt** (ADR 0023, `docs/source-input-contract.md` v1.4.0): `FIREFLY_SOURCES` ist ein JSON-Array, ein Eintrag je Quelle (`type` aus geschlossenem Vokabular `adsb_opensky`/`flarm_aprs`/`radar_asterix`, `bbox` mit Wayfinder-Feldnamen, optional `sensor_id`/`sac`/`sic`/`cred_env`/`poll_interval_secs`). `firefly-server::sources` parst die Liste per `serde_json` (`parse_sources`) — ein **unbekannter Typ** oder malformes JSON ist ein **Startfehler** (kein still ignorierter Eintrag) — und bildet einen `adsb_opensky`-Eintrag auf `OpenSkyConfig` ab (`opensky_config_from_spec`): BBox→Query-Fenster (finit/WGS84-Bereich/`min≤max`-validiert), `sensor_id` (Default wenn fehlend), **optionales `poll_interval_secs`** (nur `adsb_opensky`; nur `> 0` überschreibt, `0`/fehlend → Default 10 s — kein Heiß-Lauf; ADR 0029, Kontrakt v1.4.0, treibt via `representative_config` auch die Ausgabe-Kadenz), und die per `cred_env` **benannte** Credential-Env aufgelöst und am **ersten `:`** in `client_id:client_secret` gesplittet (UX-2, OAuth2 seit ADR 0024; fehlende/leere Cred-Env → Fehler, ohne `cred_env` → anonym). Der Klartext steht nie im JSON-Blob, nur in der benannten Env. **Verdrahtung (2b):** `build_live_state` löst die Quellen auf (`resolve_sources`: `adsb_opensky` → `OpenSkyConfig`, Radar → WARN+skip, FLARM → `FlarmConfig` (FR-NET-012), fehlerhafter Eintrag → fatal), startet **einen Poller je Quelle** in den geteilten `mpsc` des Live-Trackers, registriert **alle** Sensor-IDs im `SensorHealthMonitor` (CAT063); Referenzpunkt = Mittelpunkt der **Union** aller BBoxen, Ausgabe-Takt = **min** Poll-Intervall (`representative_config`); `FIREFLY_SOURCES` hat **Vorrang** vor `FIREFLY_OPENSKY_*` (Back-Compat-Fallback). Parse-/Config-Fehler → Prozess-Exit (Container-Fail statt Fehl-Quellen). | verifiziert (2a Parser/Mapping + 2b Live-Verdrahtung) | `firefly-server`: `sources::parses_a_mixed_source_list`, `sources::unknown_type_is_a_parse_error`, `sources::malformed_json_is_a_parse_error`, `sources::opensky_config_maps_bbox_and_sensor_id`, `sources::missing_sensor_id_keeps_the_default`, `sources::poll_interval_override_is_honored`, `sources::poll_interval_absent_or_zero_keeps_default`, `sources::poll_interval_secs_deserializes_from_json`, `sources::cred_env_is_split_into_client_id_and_secret`, `sources::cred_split_uses_the_first_colon`, `sources::cred_env_unset_or_empty_is_an_error`, `sources::cred_without_colon_is_malformed`, `sources::missing_bbox_is_an_error`, `sources::inverted_or_out_of_range_bbox_is_rejected`, `sources::resolve_splits_opensky_from_reserved_types`, `sources::resolve_propagates_a_bad_adsb_entry`, `sources::representative_unions_bboxes_and_takes_min_interval`, `sources::representative_of_empty_is_the_default` (Live-Pfad zusätzlich durch `app_test::websocket_client_receives_frames_in_live_mode` über den Fallback-Pfad abgedeckt) |
| FR-NET-013 | Firefly empfängt einen **realen Monoradar** über **ASTERIX CAT048 via UDP** und speist seine Ziel-Meldungen als **Polar-Plots** (`Measurement::Polar`, `DetectionKind`/`SourceKind` aus TYP) in denselben Tracking-Kanal wie ADS-B/FLARM (ADR 0028). Eigene Crate `firefly-radar` (Ports & Adapters): `RadarConfig` (12-Factor, `FIREFLY_RADAR_*`, plus Radar-**Standort** `lat`/`lon`/`height_m` — CAT048 ist polar und trägt ihn nicht); `target_report_to_plot` bildet einen `DecodedTargetReport` (FR-IO-005) auf einen `Plot` ab (kein Plot bei No-Detection/fehlender Position/Zeit); `datagram_to_plots`/`run` ist der UDP-Listener (Multicast-Join bei Gruppe, sonst Unicast-Bind), der je Datagramm einen CAT048-Block dekodiert und die Plots emittiert — **kein Panic** auf Eingabe, ein malformes Datagramm wird verworfen. **Verdrahtung (Schritt D):** `radar_config_from_spec` bildet einen `radar_asterix`-Quell-Eintrag auf `RadarConfig` ab (Kontrakt v1.3.0; `lat`/`lon` Pflicht, `listen`=`group:port`); `spawn_radar_listener_live` speist die Plots in den Live-Tracker; `build_live_tracker_multi` registriert Radar-Sensoren mit ihrem **eigenen Standort-Frame** + realem Polar-Fehlermodell (statt des geodätischen Platzhalters), sodass Polar-Plots korrekt ins Tracking-Frame gehoben werden (FR-TRK-010); Sensor-Health-Monitor (CAT063) und Metrik `firefly_radar_plots_received_total` (NFR-OBS-001). | verifiziert (Crate + Verdrahtung) | `firefly-radar`: `config::empty_environment_yields_defaults`, `config::valid_values_are_parsed`, `config::invalid_values_fall_back_to_defaults`, `plot::combined_report_maps_to_combined_ssr_polar_plot`, `plot::detection_classes_map_to_kind_and_source`, `plot::unmeasurable_reports_form_no_plot`, `listener::valid_datagram_yields_a_plot`, `listener::garbage_datagram_yields_no_plots`; `firefly-server`: `sources::radar_config_maps_site_identity_and_listen`, `sources::radar_without_listen_defaults_to_unicast_default_port`, `sources::radar_missing_or_out_of_range_site_is_an_error`, `sources::representative_covers_a_radar_only_feed`, `sources::resolve_splits_opensky_from_reserved_types`, `metrics::render_includes_all_metrics` |
| FR-NET-012 | Firefly empfängt **FLARM-Positionen über das Open Glider Network (OGN) via APRS-IS** und fügt sie als `Plot::adsb` (`Measurement::Geodetic`, WGS84 + isotrope Kovarianz, σ≈20 m) in denselben Tracking-Kanal ein wie ADS-B/Radar (ADR 0026). Eigene Crate `firefly-flarm` (Ports & Adapters): `FlarmConfig` (12-Factor, `FIREFLY_FLARM_*`); `parse_position` dekodiert OGN-APRS-Positions-Beacons **robust** (Fixed-Width-Position, `HHMMSS`-Tageszeit, `/A=`-Höhe, OGN-`id`-Byte `STttttaa` → Adresstyp/Aircraft-Typ, DAO-Sub-Minuten-Verfeinerung) — **kein Panic** auf Eingabe (untrusted-Pfad), Empfänger-Beacons ohne Aircraft-Adresse werden verworfen; `position_to_plot` setzt die ICAO-Adresse **nur** bei echtem ICAO-Adresstyp (FLARM/OGN-IDs sind keine ICAO-24-Adressen — bereitet #30 vor) und stempelt die **Plot-Datenzeit als volle Unix-Epoch-Sekunde** — die OGN-Tageszeit (`HHMMSSh`) wird an den UTC-Tag der Empfangs-Wanduhr verankert (mit Tageswechsel-Korrektur; Fallback Empfangszeit bei fehlendem Zeitstempel), sodass FLARM auf **derselben Uhr wie OpenSky** (`resp.time`, Epoch) läuft; andernfalls verwarf der gemeinsame monotone Datenzeit-Wasserstand des Multi-Source-Live-Trackers die FLARM-Plots als „out-of-order" und ein kombinierter ADS-B+FLARM-Feed lieferte keine Tracks (Wayfinder #120); `run` hält die APRS-IS-TCP-Verbindung (Server-Area-Filter aus der bbox, read-only anonym oder `callsign:passcode`-Login) mit Reconnect+Backoff. **Verdrahtung (Schritt C):** `flarm_config_from_spec` bildet einen `flarm_aprs`-Quell-Eintrag auf `FlarmConfig` ab (Kontrakt v1.2.0); `spawn_flarm_listener_live` speist die Plots in den Live-Tracker; der Tracker registriert **alle** Quell-Sensoren (`build_live_tracker_multi`), sodass FLARM-Plots nicht verworfen werden (FR-TRK-010); Metrik `firefly_flarm_plots_received_total` (NFR-OBS-001). | verifiziert (Schritt B Crate + Schritt C Verdrahtung) | `firefly-flarm`: `ogn::parses_a_flarm_beacon`, `ogn::parses_an_icao_beacon_without_id_field_via_prefix`, `ogn::dao_refines_the_coordinates`, `ogn::southern_and_western_hemispheres_negate`, `ogn::id_byte_bits_decode_address_and_aircraft_type`, `ogn::receiver_beacon_without_aircraft_address_is_skipped`, `ogn::server_comment_and_garbage_yield_none`, `ogn::truncations_never_panic`, `ogn::mutated_bytes_never_panic`, `plot::flarm_address_does_not_populate_icao`, `plot::icao_address_populates_icao_field`, `plot::measurement_is_geodetic_with_the_configured_sigma`, `plot::plot_time_is_unix_epoch_anchored_to_the_receive_day`, `plot::missing_beacon_time_falls_back_to_receive_time`, `plot::day_boundary_beacon_before_midnight_received_after_snaps_back`, `config::empty_environment_yields_defaults`, `config::valid_values_are_parsed`, `config::invalid_values_fall_back_to_defaults`, `aprsis::area_filter_uses_north_west_south_east_order`, `aprsis::anonymous_login_is_read_only`, `aprsis::account_login_uses_callsign_and_passcode`, `aprsis::stream_emits_one_plot_per_aircraft_line`; `firefly-server`: `sources::flarm_config_maps_bbox_sensor_and_credentials`, `sources::flarm_without_cred_is_read_only_anonymous`, `sources::flarm_missing_bbox_and_bad_cred_are_errors`, `sources::resolve_splits_opensky_from_reserved_types`, `sources::representative_covers_a_flarm_only_feed`, `metrics::render_includes_all_metrics` |

| FR-NET-015 | Firefly empfängt ADS-B von einer **eigenen Bodenstation** über **ASTERIX CAT021 via UDP** und speist die Zielmeldungen als **geodätische Plots** (`Plot::adsb`, `Measurement::Geodetic`) in denselben Tracking-Kanal wie Radar/Internet-ADS-B/FLARM (FEP.3, ARTAS-Roadmap AP-FEP — der Produktions-Bezugsweg: Push statt Poll, lokal statt Internet, gleiche Transportklasse wie das Radar). Eigene Crate `firefly-adsb021` (Ports & Adapters): `Adsb021Config` (12-Factor, `FIREFLY_ADSB021_*`; **kein Standort** — CAT021-Positionen sind WGS84-**Selbstmeldungen**, der Stations-Standort ist für die Messung irrelevant); `adsb_report_to_plot` bildet einen `DecodedAdsbReport` (FR-IO-010) auf einen `Plot` ab: **σ aus NACp je Meldung** (DO-260B, σ ≈ EPU/2: NACp 11 → 1,5 m … 1 → 9 260 m; fehlend/0 → **konservative 250 m**, bewusst schlechter als die 75-m-Annahme der Internet-Quellen — wer keine Qualität meldet, verdient weniger Vertrauen), Höhe geometrisch → barometrisch → 0 (Fallback), **Drop-Regeln**: Boden- (GBS), Simulations-/Test- (SIM/TST), positions- oder zeitlose Meldungen werden verworfen und gelangen nie ins Luftlagebild; `datagram_to_plots`/`run` ist der UDP-Listener (Multicast-Join bei Gruppe, sonst Unicast-Bind) — **kein Panic** auf Eingabe, malformes Datagramm verworfen. **Verdrahtung:** `adsb021_config_from_spec` (Kontrakt v1.6.0: `listen`?/`sac`?/`sic`?/`sensor_id`? — keine bbox, kein `cred_env`), `spawn_adsb021_listener_live` in den geteilten `mpsc`, Sensor-Default 230, Registrierung als geodätischer Sensor (Nominal 5 s), CAT063-Liveness, Metriken `firefly_adsb021_reports_received_total`/`firefly_sources_adsb021` (NFR-OBS-001). Ohne bbox trägt die Quelle nichts zum System-Referenzpunkt bei → als Einzelquelle `FIREFLY_SYSTEM_REF_*` setzen (dokumentiert). | verifiziert | `firefly-adsb021`: `config::empty_environment_yields_defaults`, `config::values_parse_and_garbage_falls_back`, `plot::airborne_report_maps_to_geodetic_plot`, `plot::nacp_table_derives_sigma`, `plot::drop_rules_keep_the_air_picture_clean`, `plot::altitude_falls_back_from_geometric_to_baro_to_zero`, `listener::valid_datagram_yields_a_plot_and_garbage_none`; `firefly-server`: `sources::adsb021_config_maps_identity_and_listen`, `sources::adsb021_minimal_spec_uses_defaults_and_needs_no_bbox_or_site`, `sources::adsb021_bad_listen_or_sac_is_an_error`, `sources::resolve_routes_adsb021_sources`, `metrics::render_includes_all_metrics` |
| FR-NET-014 | Firefly bezieht ADS-B **auth-frei** über einen **ADSBExchange-v2-kompatiblen Community-Aggregator** (adsb.lol Default, adsb.fi; ADR 0031) — zweiter ADS-B-Bezugsweg **neben** OpenSky für Umgebungen mit Datacenter-IP-Sperre oder ohne OAuth2-Client. Eigene Crate `firefly-adsbagg` (Ports & Adapters): `AdsbAggConfig` (12-Factor, `FIREFLY_ADSBAGG_*` inkl. `PROVIDER`/`BASE_URL`-Override); die konfigurierte `bbox` wird als **Umkreis** abgefragt (`circle_for_bbox`: Mittelpunkt + Haversine-Distanz zur fernsten Ecke, Floor 1 NM, **Clamp 250 NM mit WARN** — partielle Abdeckung nie verschwiegen) und die Antwort per `BBoxDeg::contains` auf die BBox **zurückgefiltert**; `parse_aircraft` bildet die Named-Fields-Records **robust** auf `Plot` ab (kein Panic auf Eingabe): `hex`→ICAO (`~`-Präfix = Nicht-ICAO/TIS-B → Plot ohne ICAO-Identität, ungültiges Hex → verworfen), `alt_baro` **Fuß oder Literal `"ground"`** (→ verworfen), `alt_geom` (Fuß) bevorzugt für die Position, `type`→σ analog ADR-0019 (`adsb*`/`adsr*` 75 m, `mlat` 200 m, sonst 300 m), `seen_pos` > 30 s → **Staleness-verworfen**, Datenzeit = Server-`now` (ms→s-normalisiert, Fallback Empfangs-Wanduhr) − `seen_pos`; 429 → `PollError::RateLimited` + exponentielles Backoff (Muster #49). **Verdrahtung:** `adsbagg_config_from_spec` (Kontrakt v1.5.0: `provider`? aus geschlossenem Vokabular — unbekannt → **Startfehler**; `cred_env` wird **ignoriert**, auth-frei), `spawn_adsbagg_poller_live` in den geteilten `mpsc`, Sensor-Default 230, CAT063-Liveness, Metriken `firefly_adsbagg_poll_errors_total`/`_rate_limited_total`/`firefly_sources_adsbagg`. | verifiziert | `firefly-adsbagg`: `config::*` (6), `geometry::frankfurt_box_yields_covering_uncapped_circle`, `geometry::oversized_box_is_clamped_and_flagged`, `geometry::degenerate_box_gets_the_minimum_radius`, `geometry::bbox_contains_is_inclusive`, `geometry::one_degree_latitude_is_about_sixty_nm`, `api::fixture_yields_the_usable_plots` (echte adsb.lol-Referenzantwort), `api::timestamps_normalise_ms_and_subtract_position_age`, `api::now_unix_secs_handles_seconds_ms_and_garbage`, `api::missing_now_uses_the_fallback_clock`, `api::unusable_records_are_dropped_not_panicked`, `api::invalid_hex_is_rejected`, `api::sigma_from_kind_follows_the_adr0019_analogue`, `api::empty_callsign_produces_none`, `poller::*` (5); `firefly-server`: `sources::aggregator_spec_deserializes_with_provider`, `sources::aggregator_config_maps_bbox_provider_and_overrides`, `sources::aggregator_defaults_provider_and_ignores_cred_env`, `sources::aggregator_unknown_provider_and_missing_bbox_are_errors`, `sources::resolve_routes_aggregator_sources`, `sources::representative_covers_an_aggregator_only_feed`, `metrics::render_includes_all_metrics` |

### Nicht-funktional (NFR)

| ID | Anforderung | Status | Nachweis |
|----|-------------|--------|----------|
| NFR-REPRO-001 | Gleicher Seed/Eingang ⇒ exakt gleicher Ausgang (Determinismus). | umgesetzt | `firefly-sim`: `reproducible_from_seed`; `firefly-server`: `replay::tests::replay_deterministic_same_input_same_tracks`, `replay_plots::replay_is_deterministic` |
| NFR-CLOUD-001 | Die Tracker-Kernlogik ist eine reine, deterministische Funktion (Zustand + Plots → Zustand + Tracks); Wanduhr/Netz/Logging bleiben außen. | verifiziert | `firefly-track`: `snapshot::replay_is_deterministic` |
| NFR-CLOUD-002 | Verarbeitung erfolgt nach Datenzeit (`dt`/Zeit wird übergeben), nicht nach Server-Uhr. | verifiziert | `firefly-track`: `snapshot::replay_is_deterministic`; `process_scan(time, …)` |
| NFR-CLOUD-003 | Track-Zustand ist serialisierbar (Snapshot) und damit wiederherstellbar/replizierbar. | verifiziert | `firefly-track`: `snapshot::snapshot_roundtrip_recovers_state`, `snapshot::restored_snapshot_continues_equivalently` |
| NFR-CLOUD-004 | Robustheit gegen schwankende/verzögerte Scan-Intervalle: Tracks werden **nicht allein aufgrund von Zeitverzug** verworfen. Lebenszyklus-Entscheidungen (Coasting/Löschung) richten sich nach Datenzeit und konfigurierbaren Schwellen, nicht nach festen Wanduhr-Fristen. Verspätete oder umsortierte Plots werden sicher verworfen statt den Track-Zustand zu korrumpieren (FR-TRK-033). | verifiziert | `firefly-track`: `timing::long_gap_with_data_keeps_track_identity`, `timing::deletion_is_governed_by_miss_count_not_elapsed_time`, `timing::unsorted_plot_batch_gives_same_result_as_sorted_batch`, `timing::late_plot_batch_is_dropped_gracefully`, `timing::backward_scan_is_dropped_gracefully` |
| NFR-OBS-001 | Strukturierte Logs, Metriken und Tracing sind vorhanden. | umgesetzt | `firefly-server`: strukturierte `tracing`-Events + `tracing-subscriber` (`app.rs`, `main.rs`); RUST_LOG-Filter. `firefly-multicast`: `tracing::debug!`/`tracing::error!` im Sender (`lib.rs::run`, pro Scan: Zeit/Bytes/Track-Zahl, Sendefehler) und `tracing::debug!`/`tracing::warn!` im Empfänger (`receiver.rs::run`, pro Block: Record-Zahl, Socket-/Decode-Fehler). `/metrics`-Endpoint (Prometheus-Textformat, `firefly-server::metrics`): `firefly_scene_frames_total` (Gauge), `firefly_ws_clients_connected` (Gauge), `firefly_ws_clients_total` (Counter), `firefly_cat062_scans_sent_total`/`firefly_cat062_send_errors_total` (Counter) — `metrics::render_includes_all_metrics`, `metrics::connected_client_guard_tracks_lifetime`, `app::metrics_endpoint_exposes_frame_count`. |
| NFR-OPS-001 | **Entfallen (ADR 0030, 2026-07-04).** Ursprünglich: Ein-Befehl-Demo mit eingebauter Szene (`FIREFLY_SCENE`) und „Verzug simulieren“-Knopf. Der Replay-/Szenen-Pfad war Lernprojekt-Altlast und wurde ausgebaut — Vorführbarkeit läuft heute über die echte Kette (Wayfinder-Stacks/Codespace mit Orchestrator-Auto-Spawn bzw. Standalone mit OpenSky-Quelle, `docs/INSTALLATION.md` §4/§7). Die Frankfurt-Mehrradar-Szene bleibt als Regressions-Fixture erhalten (`firefly-player/tests/frankfurt_regression.rs`). | entfallen | ADR 0030; Fixture-Tests `frankfurt_regression::*` |
| NFR-SAFE-001 | Kein `unsafe`-Code ohne dokumentierte Begründung. | umgesetzt | Clippy/Review-Gate (CLAUDE.md §5) |
| NFR-SAFE-002 | Coverage-geführtes Fuzzing der Vertrauensgrenzen-Parser (ARTAS-Roadmap QW.2): Die Parser an den Eingangs-Vertrauensgrenzen — CAT048-Radar-Eingang (`decode_target_reports`), die Konsum-Decoder CAT062/063/065 (`decode_data_block`/`decode_sensor_block`/`decode_status_block`) und der `FIREFLY_SOURCES`-Kontrakt-Parser (`parse_sources`) — panicken auf **keiner** Eingabe (beliebige Bytes/Strings ⇒ `Ok`/`Err`). Nachweis: fünf cargo-fuzz/libFuzzer-Targets (`fuzz/`, eigenes Workspace, Seed-Korpus aus den Referenz-Dumps) + zeitgeboxter CI-Job (`ci.yml` „Fuzz", 60 s je Target). **Erster Ertrag:** Der Fuzzer fand in Sekunden einen u8-Überlauf in der gemeinsamen FSPEC-FRN-Arithmetik (`fspec::parse`) — eine feindliche FX-Kette > 36 Oktette panickte im Debug-Build bzw. las im Release-Build stillschweigend falsche FRNs. Fix: FSPEC-Kette hart auf `MAX_FSPEC_OCTETS` = 36 begrenzt (FRN ≤ 252, jenseits jeder realen UAP), Überlänge ⇒ neuer Fehler `FspecTooLong` in allen vier Decoder-Fehlertypen. | verifiziert | `fuzz/fuzz_targets/*` (5 Targets, je ≥ 120 s lokal ohne Fund nach Fix); `firefly-asterix`: `fspec::parse_accepts_maximum_chain_with_exact_frn`, `fspec::parse_rejects_overlong_fx_chain`, `cat048::overlong_fspec_chain_is_rejected_not_panicked`, `cat062::overlong_fspec_chain_is_rejected_not_panicked`, `cat063::overlong_fspec_chain_is_rejected_not_panicked`, `cat065::overlong_fspec_chain_is_rejected_not_panicked` |
| NFR-INT-001 | Tracker-Kern ist format-/transport-neutral; Ausgabe erfolgt über einen neutralen `SystemTrack` + Adapter (Ports & Adapters). | verifiziert | `firefly-track`: `tracker::system_track_position_round_trips_through_wgs84`, `tracker::system_tracks_carry_confirmation_status` |
| NFR-INT-002 | Track-Positionen sind nach WGS84 zurückprojizierbar (geodätische Ausgabe); der Sensor-Frame wird zur Ausgabezeit übergeben (nicht im Zustand gehalten). | verifiziert | `firefly-track`: `tracker::system_track_position_round_trips_through_wgs84`; `firefly-core`: `system_track::ground_speed_is_vector_length`, `system_track::track_angle_follows_compass_convention` |
| NFR-SEC-001 | Der CAT062-Multicast-Pfad (Senden wie Empfangen) ist gegen unautorisiertes Mitlesen und Einspeisen durch **Netz-Isolation** (dediziertes, abgeschottetes Segment/VLAN, ausschließlich Firefly-Sender + autorisierte ASD-Empfänger) abgegrenzt; Multicast-TTL=1 (`MulticastConfig`-Default) ist eine zusätzliche, aber nicht hinreichende Maßnahme. Die Vertrauensgrenze liegt auf der Netzwerk-Schicht, nicht im CAT062-Anwendungsprotokoll. Zusätzlich: Der `/ws`-WebSocket-Endpunkt unterstützt opt-in Bearer-Token-Auth (`FIREFLY_WS_TOKEN`) und Origin-Prüfung (`FIREFLY_WS_ALLOWED_ORIGIN`) als anwendungsseitige Absicherung. | dokumentiert + teilverifiziert | ADR 0017; `firefly-multicast`: `config::*` (Bind-Interface, TTL=1, Gruppe/Port per Env); `firefly-server`: `app::authorize_ws`, Tests `app::tests::ws_missing_token_is_rejected_with_401`, `ws_wrong_token_is_rejected_with_401`, `ws_correct_bearer_token_is_accepted`, `ws_correct_query_token_is_accepted`, `ws_wrong_origin_is_rejected_with_403`, `ws_correct_origin_is_accepted` |

### Randbedingungen (CON)

| ID | Randbedingung | Quelle |
|----|---------------|--------|
| CON-001 | Zielplattform: Kubernetes, anbieter-neutral (souverän/On-Prem-tauglich). | ADR 0003 |
| CON-002 | Assurance-Orientierung: ED-153 (SWAL) + ED-109A/DO-278A. | ADR 0004 |
| CON-003 | Eingabe-/Austauschformat: ASTERIX (CAT048/021/062). | ADR 0001 |
| CON-004 | Code Englisch, Doku/Erklärung Deutsch. | ADR 0002 |
| CON-005 | Integrationsziel Phoenix WebInnovation (ASD/EFS); Track-Ausgabe als ASTERIX CAT062; Transport/Koordinatenbezug noch offen. | ADR 0006 |

| NFR-OBS-002 | **Erweiterte Observability (SDPS-006):** `/metrics` exponiert zusätzlich `firefly_tracks_active` (Gauge) — Anzahl der Tracks im zuletzt gesendeten CAT062-Scan. Die Metrik wird per-Scan über den `on_scan`-Callback in `firefly_multicast::run` aktualisiert (SDPS-006a). Ein importierbares Grafana-Dashboard (`monitoring/grafana/dashboard.json`) stellt alle Firefly-Metriken visualisiert bereit: Tracks-Active-Zeitreihe, CAT062-Scan-Rate, CAT065-Heartbeat-Rate, WS-Clients-Stat, Sendefehler (SDPS-006b). Hinweis: Plots/s als Echtzeit-Streaming-Metrik wird mit SDPS-001 (Live-FEP-Sensor-Ingestion) relevant; in der aktuellen Replay-Architektur sind Plots offline vorberechnet. | verifiziert | `firefly-server/src/metrics.rs` (`tracks_active`, `render`), `firefly-multicast/src/lib.rs` (`run<F>`, `on_scan`-Callback), `firefly-server/src/main.rs` (`spawn_cat062_multicast`), `monitoring/grafana/dashboard.json` | `firefly-server`: `metrics::render_includes_all_metrics` (testet `firefly_tracks_active 7` + TYPE gauge); `firefly-multicast`: `sender::sends_one_cat062_block_per_scan`, `sender::empty_scan_list_sends_nothing`, `receiver::decoded_records_match_encoded_tracks` |
| NFR-OBS-003 | **Live-Pipeline-Härtung (Betriebs-Härtung):** Die Mehrquellen-Live-Pipeline macht **Lastfestigkeit** und **Quell-Mix** beobachtbar. (1) **Back-Pressure-Verlust:** Verwirft ein Quell-Adapter (OpenSky/FLARM/Radar) einen Plot-Batch, weil der gemeinsame Quell→Tracker-Kanal (`mpsc(32)`) voll ist, zählt der Counter `firefly_live_plot_batches_dropped_total` — ein wachsender Wert ist das Operator-Signal, dass der Tracker mit der Quell-Rate nicht mitkommt und Surveillance-Daten an der Back-Pressure verloren gehen (Skalieren/Drosseln). Alle drei Spawn-Stellen (`spawn_opensky_poller_live`/`spawn_flarm_listener_live`/`spawn_radar_listener_live`) inkrementieren ihn am `try_send`-Fehlerpfad. (2) **Quell-Mix:** Die Gauges `firefly_sources_opensky`/`_flarm`/`_radar` melden die je Instanz **konfigurierte** Quell-Anzahl (in `build_live_state` einmalig gesetzt) — ein Operator bestätigt damit auf einen Blick, dass die Instanz den vom Orchestrator (ADR 0023) intendierten Quell-Mix fährt. Stützt den datenzeit-getriebenen Kern nicht an (reine Observability am Ausgabe-/Eingangs-Rand). | verifiziert | `firefly-server/src/metrics.rs` (`live_plot_batches_dropped_total`, `sources_opensky`/`_flarm`/`_radar`, `render`), `firefly-server/src/main.rs` (Gauge-Set in `build_live_state`, Drop-Inkrement in den drei Spawn-Stellen) | `firefly-server`: `metrics::render_includes_all_metrics` (prüft `firefly_live_plot_batches_dropped_total 4`, `firefly_sources_opensky/_flarm/_radar` + TYPE gauge) |

## Statuswerte

`geplant` · `in Arbeit` · `umgesetzt` · `verifiziert` (umgesetzt **und** durch
Nachweis abgesichert) · `verworfen`.
